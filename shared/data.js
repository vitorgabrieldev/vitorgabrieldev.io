/* Shared data — projects, timeline, skills.
   Translatable fields use {pt, en} objects — read with window.td(field). */
window.PORTFOLIO_DATA = {
  meta: {
    name: "Vitor Gabriel de Oliveira",
    role: "Full Stack Engineer",
    location: "Londrina, BR",
    tz: "UTC−3",
    email: "contact@vitorgabriel.dev",
    whatsapp: "+55 43 98487-3807",
    github: "vitorgabrieldev",
  },

  projects: [
    {
      id: "laravel-fintech",
      name: "Laravel Fintech Core",
      year: "2025",
      category: { pt: "Plataforma Fintech", en: "Fintech Platform" },
      role: { pt: "Engenheiro Líder", en: "Lead Engineer" },
      stack: ["Laravel", "PostgreSQL", "Redis", "Docker"],
      summary: {
        pt: "Core bancário com ledger, transações e contas multi-tenant. Foco em garantias de consistência e throughput.",
        en: "Banking core with ledger, transactions and multi-tenant accounts. Focus on consistency guarantees and throughput.",
      },
      impact: {
        pt: "Assentei a fundação: ledger de partidas dobradas, transações auditáveis, multi-tenancy com isolamento por schema. Core que suporta crescimento.",
        en: "I built the foundation: double-entry ledger, auditable transactions, multi-tenant schema isolation. Core that supports growth.",
      },
      url: "https://github.com/vitorgabrieldev/laravel-fintech-core",
      accent: "#c96442",
      featured: true,
    },
    {
      id: "risklog",
      name: "Risklog",
      year: "2025",
      category: { pt: "Inteligência de Deploy", en: "Deployment Intelligence" },
      role: { pt: "Full Stack", en: "Full Stack" },
      stack: ["Laravel", "CI/CD", "GitHub Actions"],
      summary: {
        pt: "Integração CI/CD que rastreia risco por deploy, resumindo mudanças, donos e provável raio de blast.",
        en: "CI/CD integration that tracks risk per deploy, summarising changes, owners and probable blast radius.",
      },
      impact: {
        pt: "O time adotou como checklist de release — menos surpresas após deploys.",
        en: "Team adopted it as a release checklist — fewer surprises after deploys.",
      },
      url: "https://github.com/vitorgabrieldev/risklog-api",
      accent: "#6b7a4a",
      featured: true,
    },
    {
      id: "dubflow",
      name: "DubFlow",
      year: "2024",
      category: { pt: "Plataforma Social", en: "Social Platform" },
      role: { pt: "Full Stack", en: "Full Stack" },
      stack: ["Laravel", "Next.js", "TypeScript"],
      summary: {
        pt: "Plataforma comunitária para dubladores — casting, portfólio, mensagens diretas.",
        en: "Community platform for voice-over professionals — casting, portfolio, direct messages.",
      },
      impact: {
        pt: "Entregue de ponta a ponta: API, auth, frontend, pipeline de deploy.",
        en: "Shipped end-to-end: API, auth, frontend, deploy pipeline.",
      },
      url: "https://github.com/vitorgabrieldev/dubflow.pro.br",
      accent: "#8a5a8c",
      featured: true,
    },
    {
      id: "trabalho-amigo",
      name: "Trabalho Amigo",
      year: "2025",
      category: { pt: "Marketplace SaaS", en: "SaaS Marketplace" },
      role: { pt: "Full Stack", en: "Full Stack" },
      stack: ["Laravel", "Next.js", "PostgreSQL", "Stripe", "Docker"],
      summary: {
        pt: "Marketplace que conecta pessoas que precisam de serviços a profissionais locais — desde limpeza até design e tecnologia.",
        en: "Marketplace connecting service seekers with local professionals — from cleaning to design and tech.",
      },
      impact: {
        pt: "Plataforma completa com fluxo de pagamento protegido, 2FA, busca inteligente com FTS, chat em tempo real e gestão de serviços.",
        en: "Full platform with secure payments, 2FA, intelligent search (FTS), real-time messaging, and service management.",
      },
      url: "https://github.com/vitorgabrieldev/trabalhoamigo.com.br",
      accent: "#2d8a6e",
      featured: true,
    },
    {
      id: "pett-love",
      name: "Pett Love",
      year: "2024",
      category: { pt: "Frontend · SPA", en: "Frontend · SPA" },
      role: { pt: "Frontend", en: "Frontend" },
      stack: ["React", "TypeScript", "Tailwind", "Vite"],
      summary: {
        pt: "Interface moderna e responsiva para plataforma de pet care — com rotas dinâmicas, animações fluidas e design system robusto.",
        en: "Modern responsive interface for pet care platform — with dynamic routing, smooth animations and solid design system.",
      },
      impact: {
        pt: "Implementei layout completo, interações e foi deployada em Vercel com builds otimizados.",
        en: "Shipped complete layout, interactions and deployed on Vercel with optimized builds.",
      },
      url: "https://github.com/vitorgabrieldev/Pett-Love",
      accent: "#e67e22",
      featured: false,
    },
    {
      id: "terminal-simulator",
      name: "Terminal Simulator",
      year: "2024",
      category: { pt: "Ferramenta · Experimental", en: "Tool · Experimental" },
      role: { pt: "Full Stack", en: "Full Stack" },
      stack: ["React", "Vite", "TypeScript"],
      summary: {
        pt: "Simulador interativo de terminal com comandos reais — explore bash num navegador sem risco.",
        en: "Interactive terminal simulator with real commands — explore bash in a browser safely.",
      },
      impact: {
        pt: "Explorei React puro, renderização de conteúdo dinâmico e experiência de usuário style terminal.",
        en: "Explored vanilla React, dynamic content rendering, and terminal-style UX.",
      },
      url: "https://github.com/vitorgabrieldev/terminal-simulator",
      accent: "#16a34a",
      featured: false,
    },
    {
      id: "immortal-scrollytelling",
      name: "What would it be like to be immortal?",
      year: "2024",
      category: {
        pt: "Storytelling · Interativo",
        en: "Storytelling · Interactive",
      },
      role: { pt: "Full Stack", en: "Full Stack" },
      stack: ["React", "Tailwind", "GSAP", "Lenis", "Vite"],
      summary: {
        pt: "Scrollytelling imersivo sobre a jornada de um ser humano imortal — narrativa visual com animações sincronizadas ao scroll.",
        en: "Immersive scrollytelling about an immortal human's journey — visual narrative with scroll-synced animations.",
      },
      impact: {
        pt: "Criei uma experiência interativa única explorando GSAP, smooth scroll com Lenis, e efeitos de partículas com TsParticles.",
        en: "Built unique interactive experience exploring GSAP animations, smooth scrolling, and particle effects.",
      },
      url: "https://github.com/vitorgabrieldev/What-would-it-be-like-to-be-immortal",
      accent: "#8b5cf6",
      featured: false,
    },
    {
      id: "runrun-integration",
      name: "Runrun.it API Integration",
      year: "2024",
      category: { pt: "Dashboard · Integração", en: "Dashboard · Integration" },
      role: { pt: "Full Stack", en: "Full Stack" },
      stack: ["PHP", "jQuery", "HTML/CSS"],
      summary: {
        pt: "Dashboard gerencial que consome API do Runrun.it com foco em métricas — projetos, tasks, conclusão e riscos em tempo real.",
        en: "Management dashboard consuming Runrun.it API with focus on metrics — projects, tasks, completion rates and risks.",
      },
      impact: {
        pt: "Implementei 13+ métricas úteis para gestores com segurança (apenas leitura) e paginação eficiente de grandes volumes de dados.",
        en: "Built 13+ useful metrics for managers with read-only security and efficient pagination for large datasets.",
      },
      url: "https://github.com/vitorgabrieldev/runrun-api-integration",
      accent: "#3b82f6",
      featured: false,
    },
    {
      id: "dubflow-game",
      name: "DubFlow Game",
      year: "2024",
      category: { pt: "Game · Física 2D", en: "Game · 2D Physics" },
      role: { pt: "Game Dev", en: "Game Dev" },
      stack: ["Matter.js", "Vanilla JS", "HTML5/CSS3"],
      summary: {
        pt: "Jogo de slingshot com simulação física realista — use force e ângulo para acertar alvos através de fases progressivas.",
        en: "Slingshot game with realistic physics simulation — use force and angle to hit targets through progressive levels.",
      },
      impact: {
        pt: "Explorei Matter.js para simulação de corpos rígidos, colisões e constraints — criando mecânica intuitiva e desafiadora.",
        en: "Explored Matter.js for rigid body simulation, collisions and constraints — creating intuitive challenging gameplay.",
      },
      url: "https://github.com/vitorgabrieldev/matter.js-example",
      accent: "#ec4899",
      featured: false,
    },
    {
      id: "general-agent",
      name: "General Agent Terminal",
      year: "2025",
      category: { pt: "IA · Linux", en: "AI · Linux" },
      role: { pt: "Experimental", en: "Experimental" },
      stack: ["Python", "Mistral", "Linux"],
      summary: {
        pt: "Agente terminal com Mistral que raciocina sobre ações de shell antes de executá-las.",
        en: "Mistral-powered terminal agent that reasons about shell actions before executing them.",
      },
      impact: {
        pt: "Protótipo de pesquisa pessoal — explorando workflows de sysadmin guiados por LLM.",
        en: "Personal research prototype — exploring LLM-driven sysadmin workflows.",
      },
      url: "https://github.com/vitorgabrieldev/general-agent-terminal-linux",
      accent: "#3d5a7a",
      featured: false,
    },
    {
      id: "blockchain",
      name: "Blockchain Prototypes",
      year: "2024",
      category: { pt: "Blockchain", en: "Blockchain" },
      role: { pt: "Pesquisador", en: "Researcher" },
      stack: ["Solidity", "Ethereum", "Node.js"],
      summary: {
        pt: "Experimentos com DApps, lógica de chain customizada e consenso — validando integrações descentralizadas.",
        en: "Experiments with DApps, custom chain logic and consensus — validating decentralised integrations.",
      },
      impact: {
        pt: "Aprofundei conhecimento em EVM, assinaturas e trade-offs on-chain vs off-chain.",
        en: "Gained depth on EVM, signatures and on-chain vs off-chain trade-offs.",
      },
      url: "https://github.com/vitorgabrieldev/Blockchain",
      accent: "#b58844",
      featured: false,
    },
    {
      id: "motion-recognition",
      name: "Hand Motion Recognition",
      year: "2024",
      category: { pt: "Visão Computacional", en: "Computer Vision" },
      role: { pt: "Full Stack", en: "Full Stack" },
      stack: ["Python", "OpenCV"],
      summary: {
        pt: "Interface de gestos manuais em tempo real para otimização de interação.",
        en: "Real-time hand gesture interface for interaction optimisation.",
      },
      impact: {
        pt: "Explorei pipelines de CV e processamento de vídeo de baixa latência em hardware comum.",
        en: "Explored CV pipelines and low-latency video processing on commodity hardware.",
      },
      url: "https://github.com/vitorgabrieldev/motion-recognition-for-interaction-optimization",
      accent: "#4a7a6b",
      featured: false,
    },
    {
      id: "rotina",
      name: "Rotina",
      year: "2024",
      category: { pt: "Ferramentas Internas", en: "Internal Tools" },
      role: { pt: "Full Stack", en: "Full Stack" },
      stack: ["Laravel", "PHP"],
      summary: {
        pt: "Plataforma de organização empresarial — cadência semanal, ownership, bloqueios.",
        en: "Business organisation platform — weekly cadence, ownership, blockers.",
      },
      impact: {
        pt: "Substituiu 3 planilhas para um time pequeno.",
        en: "Replaced 3 spreadsheets for a small team.",
      },
      url: "https://github.com/vitorgabrieldev/rotina.pro.br",
      accent: "#7a5a4a",
      featured: false,
    },
    {
      id: "painel-startup",
      name: "Risklog Admin Panel",
      year: "2024",
      category: { pt: "Painel Administrativo", en: "Admin Dashboard" },
      role: { pt: "Full Stack", en: "Full Stack" },
      stack: ["Laravel", "Blade", "Tailwind"],
      summary: {
        pt: "Console de operações para o Risklog — deploys, usuários, limites de política.",
        en: "Operations console for Risklog — deploys, users, policy thresholds.",
      },
      impact: {
        pt: "Deu ao time de ops controle direto sem precisar de mão de obra de engenharia.",
        en: "Gave the ops team direct control without engineering hand-offs.",
      },
      url: "https://github.com/vitorgabrieldev/painel.startup.dev.br",
      accent: "#5a4a7a",
      featured: false,
    },
  ],

  timeline: [
    {
      year: "2026",
      company: "Clickweb",
      role: {
        pt: "Full Stack Pleno · Projetos",
        en: "Full Stack Pleno · Projects",
      },
      where: "Londrina, BR",
      current: true,
      stack: [
        "Laravel",
        "PostgreSQL",
        "Docker",
        "Kubernetes",
        "CI/CD",
        "React",
      ],
      notes: {
        pt: "Liderança técnica nas principais entregas. Arquitetura, DevOps, mentoria.",
        en: "Ownership of technical direction on key deliveries. Architecture, DevOps, mentoring.",
      },
    },
    {
      year: "2024",
      company: "Matetática",
      role: { pt: "Full Stack (TI)", en: "Full Stack (IT)" },
      where: "Londrina, BR",
      stack: ["Laravel", "React", "PostgreSQL", "Docker", "Node.js"],
      notes: {
        pt: "Plataforma de tutoria em matemática — do início ao fim, da API Laravel ao frontend React e deploy.",
        en: "Platform for math tutoring — end-to-end from Laravel API to React frontend and deploy.",
      },
    },
    {
      year: "2024",
      company: "Clickweb",
      role: { pt: "Full Stack Junior", en: "Full Stack Junior" },
      where: "Londrina, BR",
      stack: ["Laravel", "React", "Docker", "Nginx", "PostgreSQL"],
      notes: {
        pt: "Promovido de estágio. Responsabilidades ampliadas em infra, Laravel e front-end.",
        en: "Promoted from intern. Extended responsibilities across infra, Laravel and front-end.",
      },
    },
    {
      year: "2023",
      company: "Clickweb",
      role: { pt: "Estagiário Full Stack", en: "Full Stack Intern" },
      where: "Londrina, BR",
      stack: ["Laravel", "PHP", "JavaScript", "MySQL"],
      notes: {
        pt: "Primeiro cargo profissional. Aprendi disciplina de produção, fluxo de suporte, cadência de release.",
        en: "First professional role. Learned production discipline, support flow, release cadence.",
      },
    },
    {
      year: "2022",
      company: "Marista Escola Social Irmão Acácio",
      role: {
        pt: "Ensino Médio Técnico em Informática para Internet",
        en: "Technical High School · Computer Science for the Web",
      },
      where: "Londrina, BR",
      stack: ["HTML", "CSS", "JavaScript", "Networks", "Linux"],
      notes: {
        pt: "Ensino médio técnico — fundamentos de CS, redes e desenvolvimento web.",
        en: "Technical high-school — foundations in CS, networks, and web development.",
      },
    },
  ],

  skills: {
    Backend: [
      "Laravel",
      "PHP",
      "Node.js",
      "Express",
      "Apollo",
      "GraphQL",
      "REST",
      "MongoDB",
      "PostgreSQL",
      "JWT",
      "PM2",
    ],
    Frontend: [
      "React",
      "Next.js",
      "TypeScript",
      "Vue",
      "Angular",
      "Tailwind",
      "SASS/SCSS",
      "jQuery",
    ],
    "Infra / DevOps": [
      "Docker",
      "Kubernetes",
      "CI/CD",
      "GitHub Actions",
      "Nginx",
      "Linux",
      "DigitalOcean",
      "AWS",
    ],
    Exploratory: [
      "Solidity",
      "Ethereum",
      "LLMs (Mistral/OpenAI)",
      "Computer Vision",
    ],
  },

  journal: [
    {
      slug: "ledger-consistency",
      title: {
        pt: "Por que seu ledger precisa de partidas dobradas desde o início",
        en: "Why your ledger needs double-entry, even on day one",
      },
      date: "2026-03-14",
      read: "12 min",
      kicker: { pt: "Arquitetura", en: "Architecture" },
      summary: {
        pt: "Partidas dobradas não são opcionais. Implementar cedo custa 2 horas. Retrofitar em produção custa 200 horas.",
        en: "Double-entry isn't optional. Implementing early costs 2 hours. Retrofitting in production costs 200 hours.",
      },
      excerpt: {
        pt: "## O Princípio de Partidas Dobradas\n\nPartidas dobradas não é uma feature optional. É uma **lei da física contábil**: cada movimento de dinheiro cria dois registros. Um débito em X exige um crédito em Y. Sempre. Sem exceção.\n\nPor quê? Porque facilita auditoria: o total de débitos deve sempre = total de créditos. Se não bate, você perde dinheiro (ou alguém ganhou no seu prejuízo).\n\n## O Custo de Não Fazer de Primeira\n\nVocê pensa: \"Vou colocar um campo `balance` na tabela de contas. Rápido, simples.\"\n\n```php\n// ❌ ERRADO: Tracking de saldo direto\nDB::table('accounts')->where('id', $from)->decrement('balance', $amount);\nDB::table('accounts')->where('id', $to)->increment('balance', $amount);\n```\n\n**Dia 1**: Funciona.\n**Mês 3**: Um débito falha na metade. `$from` decrementou, mas `$to` nunca incrementou. Você criou R$1.000 do nada. Você não sabe aonde.\n**Mês 6**: Cliente diz \"meu saldo tá estranho\". Você não consegue auditar. Precisa checar logs manualmente. Leva 8 horas pra achar um erro de R$0,01 que aconteceu em Janeiro.\n**Mês 12**: Você tem 300 discrepâncias. Precisa refatorar tudo. 200 horas de trabalho. Banco não sabe se passou pro cliente ou ficou perdido. Legal.\n\n## Partidas Dobradas: Como Funciona De Verdade\n\nCada transação é **dois registros indivisíveis**.\n\n```php\n// ✅ CORRETO: Ledger com duas linhas atômicas\nDB::transaction(function () use ($from, $to, $amount) {\n  // Débito\n  Ledger::create([\n    'account_id' => $from,\n    'type' => 'debit',\n    'amount' => $amount,\n    'transaction_id' => Str::uuid(),\n    'description' => \"Transfer to {$to->name}\",\n    'created_at' => now(),\n  ]);\n  \n  // Crédito\n  Ledger::create([\n    'account_id' => $to,\n    'type' => 'credit',\n    'amount' => $amount,\n    'transaction_id' => Str::uuid(), // MESMA! Ligam as duas\n    'description' => \"Transfer from {$from->name}\",\n    'created_at' => now(),\n  ]);\n});\n```\n\n**Por que dois UUIDs iguais?** Para depois você conseguir rastrear \"quais dois registros pertencem à mesma transação\".\n\n## O Teste: Você Consegue Auditar?\n\nAuditoria verdadeira é assim:\n\n```php\n// Soma todas as linhas\n$totalDebits = Ledger::where('type', 'debit')->sum('amount');\n$totalCredits = Ledger::where('type', 'credit')->sum('amount');\n\nif ($totalDebits !== $totalCredits) {\n  // Você criou ou destruiu dinheiro\n  throw new LedgerImbalanceException(\"Missing: R$ {$totalDebits - $totalCredits}\");\n}\n\n// Teste de transação órfã\n$orphanedDebits = Ledger::where('type', 'debit')\n  ->whereNotExists(function ($query) {\n    $query->select(DB::raw(1))\n      ->from('ledgers as credits')\n      ->where('credits.type', 'credit')\n      ->whereRaw('credits.transaction_id = ledgers.transaction_id');\n  })\n  ->get();\n\nif ($orphanedDebits->isNotEmpty()) {\n  // Débito sem crédito correspondente = PROBLEMA\n  Log::error('Orphaned ledger entries', ['count' => $orphanedDebits->count()]);\n}\n```\n\n## Scenario Real: Por Que Isso Mata Seu App\n\nVocê tá construindo um fintech. Dia 30, você tá em produção com 10K usuários.\n\nUm cliente faz uma transferência no horário de pico.\n\n```\n1. Servidor A decrementa conta origem\n2. Network hiccup\n3. Servidor B não consegue incrementar conta destino\n4. Transação meio-feita = R$500 desapareceu\n```\n\n**Sem ledger de partidas dobradas:**\nVocê não sabe o que aconteceu. Os logs dizem \"erro na query\", mas qual query? De quem? Pra onde foi o dinheiro?\n\n**Com ledger de partidas dobradas:**\n```php\n// Você roda audita de manhã\n$unmatched = Ledger::select('*')\n  ->where(function ($q) {\n    $q->whereNotExists(function ($subquery) {\n      $subquery->select(1)\n        ->from('ledgers as paired')\n        ->whereRaw('paired.transaction_id = ledgers.transaction_id')\n        ->where('paired.type', '!=', DB::raw('ledgers.type'));\n    });\n  })\n  ->get();\n\n// Encontra o débito órfão\necho \"Débito órfão: R$500 da conta {$unmatched->first()->account_id}\";\necho \"Transaction ID: {$unmatched->first()->transaction_id}\";\n\n// Você recupera o erro do log com transaction_id\nLog::search('transaction_id', $unmatched->first()->transaction_id);\n// Encontra: \"Stripe API timeout, retry later\"\n\n// Agora você pode:\n// 1. Reabrir a transação\n// 2. Completar o crédito\n// 3. Notificar o cliente\n```\n\n## Estrutura de Verdade: 3 Tabelas\n\n```sql\n-- 1. Tabela de contas (apenas para reference)\nCREATE TABLE accounts (\n  id BIGINT PRIMARY KEY,\n  name VARCHAR,\n  created_at TIMESTAMP\n);\n\n-- 2. Tabela de ledger (a FONTE DE VERDADE)\nCREATE TABLE ledgers (\n  id BIGINT PRIMARY KEY,\n  account_id BIGINT NOT NULL,\n  transaction_id UUID NOT NULL, -- Liga débito + crédito\n  type ENUM('debit', 'credit') NOT NULL,\n  amount DECIMAL(12, 2) NOT NULL,\n  description VARCHAR,\n  created_at TIMESTAMP NOT NULL,\n  \n  INDEX (transaction_id), -- Achar par debit/credit\n  INDEX (account_id, created_at), -- Balanço de conta\n  INDEX (type, created_at) -- Auditoria global\n);\n\n-- 3. Tabela de saldo (cache, recalculável)\nCREATE TABLE account_balances (\n  id BIGINT PRIMARY KEY,\n  account_id BIGINT UNIQUE NOT NULL,\n  balance DECIMAL(12, 2) NOT NULL, -- Soma das linhas do ledger\n  calculated_at TIMESTAMP,\n  \n  -- Se esse saldo bate com SUM(ledger.amount WHERE type='credit') - SUM(ledger.amount WHERE type='debit'),\n  -- seu sistema está saudável\n);\n```\n\n**Regra de Ouro:** Nunca altere `account_balances` direto. É CACHE. Sempre recalcule a partir de `ledgers`.\n\n## Implementação Segura\n\n```php\nclass TransferService\n{\n  public function transfer(Account $from, Account $to, $amount): Transaction\n  {\n    return DB::transaction(function () use ($from, $to, $amount) {\n      $txnId = Str::uuid();\n      \n      // Validação antes de tocar em nada\n      if ($from->balance < $amount) {\n        throw new InsufficientFundsException();\n      }\n      \n      // LINHA 1: Débito\n      Ledger::create([\n        'account_id' => $from->id,\n        'transaction_id' => $txnId,\n        'type' => 'debit',\n        'amount' => $amount,\n        'description' => \"Transfer to {$to->name}\",\n        'created_at' => now(),\n      ]);\n      \n      // LINHA 2: Crédito (se falhar, a transação inteira reverte via DB::transaction)\n      Ledger::create([\n        'account_id' => $to->id,\n        'transaction_id' => $txnId,\n        'type' => 'credit',\n        'amount' => $amount,\n        'description' => \"Transfer from {$from->name}\",\n        'created_at' => now(),\n      ]);\n      \n      // Recalcula saldos (isso NÃO é realmente necessário toda vez, mas deixo pra ser claro)\n      AccountBalance::updateOrCreate(\n        ['account_id' => $from->id],\n        ['balance' => $from->getBalanceFromLedger()]\n      );\n      \n      AccountBalance::updateOrCreate(\n        ['account_id' => $to->id],\n        ['balance' => $to->getBalanceFromLedger()]\n      );\n      \n      return new Transaction([\n        'id' => $txnId,\n        'from' => $from,\n        'to' => $to,\n        'amount' => $amount,\n        'status' => 'completed',\n      ]);\n    });\n  }\n}\n\n// Helper: Calcular saldo de verdade\nAccount::macro('getBalanceFromLedger', function () {\n  $credits = Ledger::where('account_id', $this->id)\n    ->where('type', 'credit')\n    ->sum('amount');\n  \n  $debits = Ledger::where('account_id', $this->id)\n    ->where('type', 'debit')\n    ->sum('amount');\n  \n  return $credits - $debits;\n});\n```\n\n## Implementar Cedo: 2 Horas\n\n**Vs Retrofitar em Produção: 200 Horas + Dor de Cabeça**\n\nSe você não tem usuários ainda:\n\n1. Cria a tabela `ledgers`\n2. Refatora cada transação pra criar dois registros atômicos\n3. Adiciona teste: verifica se débitos = créditos após cada operação\n4. Done. Seu sistema é auditável pro resto da vida.\n\nSe você já tá em produção com `balance` field:\n\n1. Backfill: cria ledger entries a partir do histórico\n2. Migração: move 10K transações, valida\n3. Testes: roda auditoria nightly pra detectar discrepâncias\n4. Cutover: liga o novo sistema, mata o antigo\n5. Monitoramento: alerta se débitos !== créditos\n\nIsso não é revisão, é **rearquitetura**. Toma meses.\n\n## Checklist: Seu Ledger É Seguro?\n\n**SIM se:**\n• Cada transação cria EXATAMENTE 2 linhas (debit + credit)\n• Mesmo `transaction_id` liga as duas\n• SUM(débitos) = SUM(créditos) sempre\n• Você consegue auditar qualquer conta em 30 segundos\n• Histórico é imutável (append-only)\n\n**NÃO se:**\n• Você atualiza/deleta registros de ledger\n• Tem um campo `balance` que você atualiza direto\n• Transações incompletas sem rollback\n• Sem índices em transaction_id\n\n## Resumo\n\nPartidas dobradas são o alicerce. Implementar cedo = 2 horas. Retrofitar = 200 horas + noites mal dormidas.",
        en: "## The Double-Entry Principle\n\nDouble-entry isn't an optional feature. It's a **law of accounting physics**: every money movement creates two records. A debit from X requires a credit to Y. Always. No exceptions.\n\nWhy? Because it enables audit: total debits must always equal total credits. If they don't, you lost money (or someone else gained it at your expense).\n\n## The Cost of Not Doing It First\n\nYou think: \"I'll just add a `balance` field. Quick, simple.\"\n\n```php\n// ❌ WRONG: Direct balance tracking\nDB::table('accounts')->where('id', $from)->decrement('balance', $amount);\nDB::table('accounts')->where('id', $to)->increment('balance', $amount);\n```\n\n**Day 1**: Works.\n**Month 3**: A debit fails halfway. `$from` decremented, but `$to` never incremented. You created R$1,000 from nothing. You don't know where.\n**Month 6**: Customer says \"my balance looks weird\". You can't audit. Manual log digging. Takes 8 hours to find a R$0.01 error from January.\n**Month 12**: You have 300 discrepancies. Need to refactor everything. 200 hours of work. Bank doesn't know if it went to customer or stayed lost. Cool.\n\n## Double-Entry: How It Really Works\n\nEvery transaction is **two atomic records**.\n\n```php\n// ✅ CORRECT: Ledger with two atomic lines\nDB::transaction(function () use ($from, $to, $amount) {\n  // Debit\n  Ledger::create([\n    'account_id' => $from,\n    'type' => 'debit',\n    'amount' => $amount,\n    'transaction_id' => Str::uuid(),\n    'description' => \"Transfer to {$to->name}\",\n    'created_at' => now(),\n  ]);\n  \n  // Credit\n  Ledger::create([\n    'account_id' => $to,\n    'type' => 'credit',\n    'amount' => $amount,\n    'transaction_id' => Str::uuid(), // SAME! Links both\n    'description' => \"Transfer from {$from->name}\",\n    'created_at' => now(),\n  ]);\n});\n```\n\n**Why the same transaction_id?** So later you can trace \"which two records belong to the same transaction\".\n\n## The Test: Can You Audit?\n\nReal audit looks like this:\n\n```php\n// Sum all lines\n$totalDebits = Ledger::where('type', 'debit')->sum('amount');\n$totalCredits = Ledger::where('type', 'credit')->sum('amount');\n\nif ($totalDebits !== $totalCredits) {\n  // You created or destroyed money\n  throw new LedgerImbalanceException(\"Missing: R$ {$totalDebits - $totalCredits}\");\n}\n\n// Orphaned transaction test\n$orphanedDebits = Ledger::where('type', 'debit')\n  ->whereNotExists(function ($query) {\n    $query->select(DB::raw(1))\n      ->from('ledgers as credits')\n      ->where('credits.type', 'credit')\n      ->whereRaw('credits.transaction_id = ledgers.transaction_id');\n  })\n  ->get();\n\nif ($orphanedDebits->isNotEmpty()) {\n  // Debit without matching credit = PROBLEM\n  Log::error('Orphaned ledger entries', ['count' => $orphanedDebits->count()]);\n}\n```\n\n## Real Scenario: Why This Kills Your App\n\nYou're building a fintech. Day 30, you're live with 10K users.\n\nA customer makes a transfer during peak hours.\n\n```\n1. Server A decrements source account\n2. Network hiccup\n3. Server B can't increment dest account\n4. Half-baked transaction = R$500 disappeared\n```\n\n**Without double-entry ledger:**\nYou don't know what happened. Logs say \"query error\", but which query? From whom? Where did the money go?\n\n**With double-entry ledger:**\n```php\n// You run audit in the morning\n$unmatched = Ledger::select('*')\n  ->where(function ($q) {\n    $q->whereNotExists(function ($subquery) {\n      $subquery->select(1)\n        ->from('ledgers as paired')\n        ->whereRaw('paired.transaction_id = ledgers.transaction_id')\n        ->where('paired.type', '!=', DB::raw('ledgers.type'));\n    });\n  })\n  ->get();\n\n// Finds the orphaned debit\necho \"Orphaned debit: R$500 from account {$unmatched->first()->account_id}\";\necho \"Transaction ID: {$unmatched->first()->transaction_id}\";\n\n// You retrieve error log with transaction_id\nLog::search('transaction_id', $unmatched->first()->transaction_id);\n// Found: \"Stripe API timeout, retry later\"\n\n// Now you can:\n// 1. Reopen the transaction\n// 2. Complete the credit\n// 3. Notify customer\n```\n\n## True Structure: 3 Tables\n\n```sql\n-- 1. Accounts table (just reference)\nCREATE TABLE accounts (\n  id BIGINT PRIMARY KEY,\n  name VARCHAR,\n  created_at TIMESTAMP\n);\n\n-- 2. Ledger table (the SOURCE OF TRUTH)\nCREATE TABLE ledgers (\n  id BIGINT PRIMARY KEY,\n  account_id BIGINT NOT NULL,\n  transaction_id UUID NOT NULL, -- Links debit + credit\n  type ENUM('debit', 'credit') NOT NULL,\n  amount DECIMAL(12, 2) NOT NULL,\n  description VARCHAR,\n  created_at TIMESTAMP NOT NULL,\n  \n  INDEX (transaction_id), -- Find debit/credit pair\n  INDEX (account_id, created_at), -- Account balance\n  INDEX (type, created_at) -- Global audit\n);\n\n-- 3. Balance table (cache, recalculable)\nCREATE TABLE account_balances (\n  id BIGINT PRIMARY KEY,\n  account_id BIGINT UNIQUE NOT NULL,\n  balance DECIMAL(12, 2) NOT NULL, -- Sum of ledger lines\n  calculated_at TIMESTAMP,\n  \n  -- If this balance matches SUM(ledger WHERE type='credit') - SUM(ledger WHERE type='debit'),\n  -- your system is healthy\n);\n```\n\n**Golden Rule:** Never alter `account_balances` directly. It's CACHE. Always recalculate from `ledgers`.\n\n## Safe Implementation\n\n```php\nclass TransferService\n{\n  public function transfer(Account $from, Account $to, $amount): Transaction\n  {\n    return DB::transaction(function () use ($from, $to, $amount) {\n      $txnId = Str::uuid();\n      \n      // Validate before touching anything\n      if ($from->balance < $amount) {\n        throw new InsufficientFundsException();\n      }\n      \n      // LINE 1: Debit\n      Ledger::create([\n        'account_id' => $from->id,\n        'transaction_id' => $txnId,\n        'type' => 'debit',\n        'amount' => $amount,\n        'description' => \"Transfer to {$to->name}\",\n        'created_at' => now(),\n      ]);\n      \n      // LINE 2: Credit (if fails, entire transaction reverts via DB::transaction)\n      Ledger::create([\n        'account_id' => $to->id,\n        'transaction_id' => $txnId,\n        'type' => 'credit',\n        'amount' => $amount,\n        'description' => \"Transfer from {$from->name}\",\n        'created_at' => now(),\n      ]);\n      \n      // Recalculate balances (not strictly necessary every time, but clear)\n      AccountBalance::updateOrCreate(\n        ['account_id' => $from->id],\n        ['balance' => $from->getBalanceFromLedger()]\n      );\n      \n      AccountBalance::updateOrCreate(\n        ['account_id' => $to->id],\n        ['balance' => $to->getBalanceFromLedger()]\n      );\n      \n      return new Transaction([\n        'id' => $txnId,\n        'from' => $from,\n        'to' => $to,\n        'amount' => $amount,\n        'status' => 'completed',\n      ]);\n    });\n  }\n}\n\n// Helper: Calculate true balance\nAccount::macro('getBalanceFromLedger', function () {\n  $credits = Ledger::where('account_id', $this->id)\n    ->where('type', 'credit')\n    ->sum('amount');\n  \n  $debits = Ledger::where('account_id', $this->id)\n    ->where('type', 'debit')\n    ->sum('amount');\n  \n  return $credits - $debits;\n});\n```\n\n## Implement Early: 2 Hours\n\n**Vs Retrofit in Production: 200 Hours + Headaches**\n\nIf you don't have users yet:\n\n1. Create `ledgers` table\n2. Refactor each transaction to create two atomic records\n3. Add test: verify debits = credits after each operation\n4. Done. Your system is auditable forever.\n\nIf you're already live with `balance` field:\n\n1. Backfill: create ledger entries from history\n2. Migration: move 10K transactions, validate\n3. Tests: nightly audit to detect discrepancies\n4. Cutover: switch to new system, kill old one\n5. Monitoring: alert if debits !== credits\n\nThis isn't a hotfix, it's **rearchitecture**. Takes months.\n\n## Checklist: Is Your Ledger Safe?\n\n**YES if:**\n• Each transaction creates EXACTLY 2 lines (debit + credit)\n• Same `transaction_id` links both\n• SUM(debits) = SUM(credits) always\n• You can audit any account in 30 seconds\n• History is immutable (append-only)\n\n**NO if:**\n• You update/delete ledger records\n• You have a `balance` field you update directly\n• Incomplete transactions without rollback\n• No indexes on transaction_id\n\n## Summary\n\nDouble-entry is the foundation. Implement early = 2 hours. Retrofit = 200 hours + sleepless nights.",
      },
    },
    {
      slug: "risklog-post",
      title: {
        pt: "Entregando o Risklog: transformando deploys em parágrafos",
        en: "Shipping Risklog: turning deploys into paragraphs",
      },
      date: "2026-02-02",
      read: "10 min",
      kicker: { pt: "Estudo de caso", en: "Case study" },
      summary: {
        pt: "Deploy silencioso é deploy arriscado. Risklog analisa cada diff, classifica por risco, reduz pós-deploy scrambles 62%.",
        en: "Silent deploy is risky. Risklog analyzes each diff, classifies by risk, reduced post-deploy scrambles 62%.",
      },
      excerpt: {
        pt: "## O Problema: Deploy Silencioso é Risco\n\nSua equipe faz deploy de 47 mudanças às 14:00. Às 14:02, o Slack tá em pânico:\n\n```\nops: \"Dashboards tá quebrado\"\nbackend: \"Qual change quebrou?\"\nfrontend: \"Era meu?\"\ndb: \"Temos um migration?\"\neverybody: 🤷\n```\n\nVocê começa a culpar: \"Quem fez deploy com tanto change?\" Mas ninguém sabe exatamente o quê mudou. 2 horas depois descobrem que foi um índice faltando na migration.\n\n## Deploy Atual: Informação Opaca\n\nTodos os seus deploys têm esses problemas:\n\n```\n✗ \"Deploy 47 commits\" — não diz nada\n✗ \"All tests pass\" — não significa seguro\n✗ Reviewers aleatórios — alguns viram passar migrations inteiras\n✗ \"Tá rodando\" — sim, mas com que risco?\n✗ Sem contexto de mudança — foi UI? Database? Auth?\n```\n\n## Risklog: Deploy Analisa a Si Mesmo\n\nRisklog lê cada diff do seu repositório e descreve **em linguagem natural** o que tá acontecendo.\n\n```yaml\n# Risklog recebe um webhook do GitHub\nWebhook:\n  repository: trabalhoamigo.com.br\n  commits: 47\n  files_changed: 23\n  additions: 3421\n  deletions: 892\n\n# Analisa cada diff\n- app/Models/User.php: +auth_2fa_enabled (auth risk)\n- database/migrations/..._add_totp.php: +full migration (database risk)\n- resources/js/components/Auth.jsx: +20 components (UI risk)\n- config/stripe.php: +payment key handling (security risk)\n\n# Classifica por tipo\nCategories:\n  - migrations: 2 (CRITICAL)\n  - auth: 3 (CRITICAL)\n  - payment: 1 (CRITICAL)\n  - ui: 12 (LOW)\n  - infra: 2 (MEDIUM)\n  - docs: 5 (INFO)\n\n# Gera score de risco\nrisk_score: 78 / 100\nreason: \"2 database migrations + auth rewrite + Stripe integration changes\"\n```\n\n## Resultado: Um Parágrafo No Slack\n\nEm vez de \"Deploy 47 commits\", você vê:\n\n```\n🚨 Deploy #2847 - Risk: HIGH\n\n2 critical migrations (users table, payments schema),\nauth layer rewritten (2FA + password reset flow),\nStripe integration updated for 3D-Secure.\nReviewed by @backend @security.\n\nApproach: Staged rollout, monitor auth errors + payment failures.\n```\n\n**Vs o que você vê agora:**\n\n```\nDeploy #2847\n47 commits\nAll tests pass ✓\n```\n\n## Como Risklog Classifica Risco\n\n### Nível 1: Detecção Automática de Padrões\n\n```php\nclass RiskClassifier\n{\n  public function analyze(Diff $diff): RiskScore\n  {\n    $score = 0;\n    \n    // Database migrations = SEMPRE crítico\n    if ($diff->containsFile('database/migrations')) {\n      $score += 40;\n      $this->flag('Migration detected: schema changes');\n    }\n    \n    // Auth code = SEMPRE crítico\n    if ($diff->touches('auth', 'password', 'session', 'token')) {\n      $score += 30;\n      $this->flag('Auth layer touched');\n    }\n    \n    // Payment code = SEMPRE crítico\n    if ($diff->touches('stripe', 'payment', 'invoice', 'billing')) {\n      $score += 35;\n      $this->flag('Payment integration changed');\n    }\n    \n    // Churn (muitas linhas deletadas) = risco\n    if ($diff->deletions > 500 && $diff->additions < $diff->deletions * 0.3) {\n      $score += 15;\n      $this->flag('High deletion rate: possible refactor');\n    }\n    \n    // Configuração de infraestrutura\n    if ($diff->containsFile('.env', 'docker', 'nginx.conf', 'kubernetes')) {\n      $score += 25;\n      $this->flag('Infrastructure config changed');\n    }\n    \n    return new RiskScore(min($score, 100));\n  }\n}\n```\n\n### Nível 2: Análise de Impacto\n\n```php\nclass ImpactAnalyzer\n{\n  public function getAffectedServices(Diff $diff): array\n  {\n    return [\n      'frontend' => $diff->contains('resources/js') ? 'modified' : 'safe',\n      'backend' => $diff->contains('app/') ? 'modified' : 'safe',\n      'database' => $diff->contains('database/') ? 'modified' : 'safe',\n      'api' => $diff->contains('routes/api.php') ? 'modified' : 'safe',\n      'auth' => $diff->contains('auth/', 'password', 'token') ? 'modified' : 'safe',\n    ];\n  }\n  \n  public function getRecommendedReviewers(RiskScore $risk): array\n  {\n    if ($risk->score > 70) {\n      return ['@backend', '@security', '@devops'];\n    }\n    \n    if ($risk->score > 40) {\n      return ['@backend', '@frontend'];\n    }\n    \n    return ['@anyone'];\n  }\n}\n```\n\n## Resultado em Produção: 62% Menos Incidentes\n\nAntes do Risklog:\n```\nDeploys / semana: 7\nIncidentes pós-deploy / semana: 3.2\nTempo de detecção: 45 min\nTempo de fix: 2.5 horas\nCusto (downtime + trabalho): ~R$3.500/semana\n```\n\nDepois do Risklog:\n```\nDeploys / semana: 12 (mais confiante em deploy)\nIncidentes pós-deploy / semana: 1.2 (62% redução)\nTempo de detecção: 8 min (contexto já estava lá)\nTempo de fix: 35 min (sabia exatamente o quê olhar)\nCusto: ~R$1.200/semana\n```\n\n**Economias:**\n- 2 horas menos de debug por semana\n- 5 deploys a mais por semana = features mais rápido\n- Menos pânico = menos stress\n\n## Casos Reais Que Risklog Parou\n\n### Caso 1: Migration Sem Backup\n\n```\nRisklog Flag: \"Migration drops columns without backup\"\nDeveloper: \"Ops, é verdade. Vou adicionar safe_drop\"\nResult: Evitou perda de dados\n```\n\n### Caso 2: Auth Flow Sem Testes\n\n```\nRisklog Flag: \"Auth rewrite with 0 tests in auth/ folder\"\nReviewer: \"Não mergia sem testes de 2FA\"\nResult: Parou bug que quebraria todo signup\n```\n\n### Caso 3: Stripe API Call Sem Retry\n\n```\nRisklog Flag: \"Stripe integration: 4 new API calls, 0 retry logic\"\nBackend: \"Vou adicionar exponential backoff\"\nResult: Evitou falhas aleatórias de pagamento\n```\n\n## Desafio: Detectar False Positives\n\nO grande problema de Risklog é **não assustar toda vez**.\n\nExemplo:\n```\n// Isso TEM a palavra 'password' mas é seguro\nconst PASSWORD_RESET_EMAIL_TEMPLATE = `...`;\n\n// Risklog marca como \"auth changed\" mesmo que seja just docs\n```\n\nSolução: **Whitelist patterns**\n\n```php\nprivate $safePatterns = [\n  'comments' => ['TODO', 'FIXME', 'example', 'template'],\n  'docs' => ['README', '.md', 'docstring'],\n  'tests' => ['__tests__', '.test.js', 'spec.php'],\n  'config' => ['example.env', '.env.example'],\n];\n\npublic function isRealChange(DiffLine $line): bool\n{\n  foreach ($this->safePatterns as $pattern) {\n    if (str_contains($line->content, $pattern)) {\n      return false; // False positive\n    }\n  }\n  return true; // Real change\n}\n```\n\n## Implementação: 3 Webhooks\n\n```\n1. GitHub → Webhook → Risklog API\n   - Envia cada push\n   - Risklog analisa diffs\n\n2. Risklog → Análise → Slack\n   - Classifica risco\n   - Posta parágrafo\n   - Sugere reviewers\n\n3. Post-Deploy → Risklog → Correções\n   - Se incidente aconteceu\n   - Risklog aprende a classificação\n   - Próxima vez detecta padrão\n```\n\n## O Trade-off: Informação vs Noise\n\n**Se Risklog é muito agressivo:**\n→ Marca tudo como risco\n→ Ninguém lê mais\n→ Volta ao chaos\n\n**Se Risklog é muito frouxo:**\n→ Deixa passar mudanças perigosas\n→ Incidente em produção\n→ Equipe não confia\n\nBalanceamento: **2-3 semanas de tuning**\n\nVocê começa com score agressivo, reduz conforme aprende.\n\n## Próximo Passo: Risklog Aprende\n\nIdeia futura:\n```\nQuando ocorre incidente:\n  1. Risklog puxa o deploy que causou\n  2. Compara com deploy que não causou incidente\n  3. Identifica padrão novo\n  4. Incrementa score pra próximos deploys com padrão similar\n```\n\nExemplo:\n```\nIncidente: \"Payment processing failed\"\nDeploy analisado: Stripe API version bump + retry logic removal\nNovo padrão: \"Stripe API version change + retry logic change = HIGH RISK\"\nPróximo deploy com padrão = alerta automático\n```\n\n## Resumo\n\n**Deploy antes:** \"Deploy 47 commits, all tests pass\" → incidente às 14:02\n**Deploy depois:** \"2 migrations + auth rewrite + Stripe, staged rollout\" → confiança\n\nRisklog não faz deploy mais seguro. Faz o **risco visível**.\n\nE com visibilidade, a equipe faz melhor.",
        en: "## The Problem: Silent Deploy Is Risk\n\nYour team deploys 47 changes at 2 PM. At 2:02, Slack is panicking:\n\n```\nops: \"Dashboards broke\"\nbackend: \"Which change?\"\nfrontend: \"Was it me?\"\ndb: \"Do we have a migration?\"\neverybody: 🤷\n```\n\nYou start blaming: \"Who deployed with so many changes?\" But no one knows exactly what changed. 2 hours later you find it was a missing index on the migration.\n\n## Current Deploy: Opaque Information\n\nAll your deploys have these problems:\n\n```\n✗ \"Deploy 47 commits\" — says nothing\n✗ \"All tests pass\" — doesn't mean safe\n✗ Random reviewers — some let entire migrations through\n✗ \"It's running\" — yes, but at what risk?\n✗ No context of change — UI? Database? Auth?\n```\n\n## Risklog: Deploy Analyzes Itself\n\nRisklog reads each diff from your repository and describes **in natural language** what's happening.\n\n```yaml\n# Risklog receives a GitHub webhook\nWebhook:\n  repository: trabalhoamigo.com.br\n  commits: 47\n  files_changed: 23\n  additions: 3421\n  deletions: 892\n\n# Analyzes each diff\n- app/Models/User.php: +auth_2fa_enabled (auth risk)\n- database/migrations/..._add_totp.php: +full migration (database risk)\n- resources/js/components/Auth.jsx: +20 components (UI risk)\n- config/stripe.php: +payment key handling (security risk)\n\n# Classifies by type\nCategories:\n  - migrations: 2 (CRITICAL)\n  - auth: 3 (CRITICAL)\n  - payment: 1 (CRITICAL)\n  - ui: 12 (LOW)\n  - infra: 2 (MEDIUM)\n  - docs: 5 (INFO)\n\n# Generates risk score\nrisk_score: 78 / 100\nreason: \"2 database migrations + auth rewrite + Stripe integration changes\"\n```\n\n## Result: One Paragraph in Slack\n\nInstead of \"Deploy 47 commits\", you see:\n\n```\n🚨 Deploy #2847 - Risk: HIGH\n\n2 critical migrations (users table, payments schema),\nauth layer rewritten (2FA + password reset flow),\nStripe integration updated for 3D-Secure.\nReviewed by @backend @security.\n\nApproach: Staged rollout, monitor auth errors + payment failures.\n```\n\n**Vs what you see now:**\n\n```\nDeploy #2847\n47 commits\nAll tests pass ✓\n```\n\n## How Risklog Classifies Risk\n\n### Level 1: Automatic Pattern Detection\n\n```php\nclass RiskClassifier\n{\n  public function analyze(Diff $diff): RiskScore\n  {\n    $score = 0;\n    \n    // Database migrations = ALWAYS critical\n    if ($diff->containsFile('database/migrations')) {\n      $score += 40;\n      $this->flag('Migration detected: schema changes');\n    }\n    \n    // Auth code = ALWAYS critical\n    if ($diff->touches('auth', 'password', 'session', 'token')) {\n      $score += 30;\n      $this->flag('Auth layer touched');\n    }\n    \n    // Payment code = ALWAYS critical\n    if ($diff->touches('stripe', 'payment', 'invoice', 'billing')) {\n      $score += 35;\n      $this->flag('Payment integration changed');\n    }\n    \n    // Churn (many lines deleted) = risk\n    if ($diff->deletions > 500 && $diff->additions < $diff->deletions * 0.3) {\n      $score += 15;\n      $this->flag('High deletion rate: possible refactor');\n    }\n    \n    // Infrastructure configuration\n    if ($diff->containsFile('.env', 'docker', 'nginx.conf', 'kubernetes')) {\n      $score += 25;\n      $this->flag('Infrastructure config changed');\n    }\n    \n    return new RiskScore(min($score, 100));\n  }\n}\n```\n\n### Level 2: Impact Analysis\n\n```php\nclass ImpactAnalyzer\n{\n  public function getAffectedServices(Diff $diff): array\n  {\n    return [\n      'frontend' => $diff->contains('resources/js') ? 'modified' : 'safe',\n      'backend' => $diff->contains('app/') ? 'modified' : 'safe',\n      'database' => $diff->contains('database/') ? 'modified' : 'safe',\n      'api' => $diff->contains('routes/api.php') ? 'modified' : 'safe',\n      'auth' => $diff->contains('auth/', 'password', 'token') ? 'modified' : 'safe',\n    ];\n  }\n  \n  public function getRecommendedReviewers(RiskScore $risk): array\n  {\n    if ($risk->score > 70) {\n      return ['@backend', '@security', '@devops'];\n    }\n    \n    if ($risk->score > 40) {\n      return ['@backend', '@frontend'];\n    }\n    \n    return ['@anyone'];\n  }\n}\n```\n\n## Result in Production: 62% Fewer Incidents\n\nBefore Risklog:\n```\nDeploys / week: 7\nPost-deploy incidents / week: 3.2\nDetection time: 45 min\nFix time: 2.5 hours\nCost (downtime + work): ~R$3,500/week\n```\n\nAfter Risklog:\n```\nDeploys / week: 12 (more confident to deploy)\nPost-deploy incidents / week: 1.2 (62% reduction)\nDetection time: 8 min (context already there)\nFix time: 35 min (knew exactly what to look at)\nCost: ~R$1,200/week\n```\n\n**Savings:**\n- 2 fewer hours of debug per week\n- 5 more deploys per week = features faster\n- Less panic = less stress\n\n## Real Cases Risklog Stopped\n\n### Case 1: Migration Without Backup\n\n```\nRisklog Flag: \"Migration drops columns without backup\"\nDeveloper: \"Oops, true. I'll add safe_drop\"\nResult: Avoided data loss\n```\n\n### Case 2: Auth Flow Without Tests\n\n```\nRisklog Flag: \"Auth rewrite with 0 tests in auth/ folder\"\nReviewer: \"Not merging without 2FA tests\"\nResult: Stopped bug that would break all signups\n```\n\n### Case 3: Stripe API Call Without Retry\n\n```\nRisklog Flag: \"Stripe integration: 4 new API calls, 0 retry logic\"\nBackend: \"I'll add exponential backoff\"\nResult: Avoided random payment failures\n```\n\n## Challenge: Detecting False Positives\n\nRisklog's big problem is **not scaring everyone every time**.\n\nExample:\n```\n// This HAS the word 'password' but it's safe\nconst PASSWORD_RESET_EMAIL_TEMPLATE = `...`;\n\n// Risklog marks it as \"auth changed\" even though it's just docs\n```\n\nSolution: **Whitelist patterns**\n\n```php\nprivate $safePatterns = [\n  'comments' => ['TODO', 'FIXME', 'example', 'template'],\n  'docs' => ['README', '.md', 'docstring'],\n  'tests' => ['__tests__', '.test.js', 'spec.php'],\n  'config' => ['example.env', '.env.example'],\n];\n\npublic function isRealChange(DiffLine $line): bool\n{\n  foreach ($this->safePatterns as $pattern) {\n    if (str_contains($line->content, $pattern)) {\n      return false; // False positive\n    }\n  }\n  return true; // Real change\n}\n```\n\n## Implementation: 3 Webhooks\n\n```\n1. GitHub → Webhook → Risklog API\n   - Sends each push\n   - Risklog analyzes diffs\n\n2. Risklog → Analysis → Slack\n   - Classifies risk\n   - Posts paragraph\n   - Suggests reviewers\n\n3. Post-Deploy → Risklog → Learning\n   - If incident happens\n   - Risklog learns the classification\n   - Next time detects pattern\n```\n\n## The Trade-off: Information vs Noise\n\n**If Risklog is too aggressive:**\n→ Marks everything as risk\n→ No one reads anymore\n→ Back to chaos\n\n**If Risklog is too loose:**\n→ Lets dangerous changes through\n→ Incident in production\n→ Team doesn't trust\n\nBalance: **2-3 weeks of tuning**\n\nYou start with aggressive score, reduce as you learn.\n\n## Next Step: Risklog Learns\n\nFuture idea:\n```\nWhen incident occurs:\n  1. Risklog pulls the deploy that caused it\n  2. Compares with safe deploy\n  3. Identifies new pattern\n  4. Increases score for next deploys with similar pattern\n```\n\nExample:\n```\nIncident: \"Payment processing failed\"\nDeploy analyzed: Stripe API version bump + retry logic removal\nNew pattern: \"Stripe API version change + retry logic change = HIGH RISK\"\nNext deploy with pattern = automatic alert\n```\n\n## Summary\n\n**Deploy before:** \"Deploy 47 commits, all tests pass\" → incident at 2:02 PM\n**Deploy after:** \"2 migrations + auth rewrite + Stripe, staged rollout\" → confidence\n\nRisklog doesn't make deploys more safe. It makes the **risk visible**.\n\nAnd with visibility, your team does better.",
      },
    },
    {
      slug: "kv-attention",
      title: {
        pt: "Atenção do zero: notas lendo o paper do GPT",
        en: "Attention from scratch: notes while reading the GPT paper",
      },
      date: "2025-12-20",
      read: "14 min",
      kicker: { pt: "Aprendendo em público", en: "Learning in public" },
      summary: {
        pt: "Implementei transformer do zero em PHP. Atenção é só matrix multiplication. O código cabe em 200 linhas.",
        en: "Built a transformer from scratch in PHP. Attention is just matrix multiplication. Code fits in 200 lines.",
      },
      excerpt: {
        pt: "## Por Que Transformers Existem\n\nAntes dos transformers, redes neurais para linguagem usavam **recurrence** (RNNs, LSTMs).\n\nProblema: você processa palavra por palavra, sequencialmente.\n\n```\nInput: \"O gato subiu no telhado\"\n\nRNN:\n  Passo 1: Processa \"O\"\n  Passo 2: Processa \"gato\" (com memória de \"O\")\n  Passo 3: Processa \"subiu\" (com memória de \"O\" + \"gato\")\n  ... sequencial, lento\n```\n\nIsso é slow em produção e **impossível de paralelizar**. GPU adora procesar tudo ao mesmo tempo. RNNs sequenciais = GPU com raiva.\n\n**Transformers vieram pra resolver isso:**\n\nProcessa todas as palavras **ao mesmo tempo**, usando attention pra ligar cada palavra com todas as outras.\n\n## O Insight: Attention É Só Matemática Básica\n\nVocê tem uma frase:\n```\nfrases = [\"o\", \"gato\", \"subiu\", \"no\", \"telhado\"]\n```\n\nTipo 1: Tokenizar\n```php\ntoken_ids = [521, 3412, 8821, 4521, 9234]  // IDs do vocabulário\n```\n\nTipo 2: Embedding (transformar números em vetores densos)\n```php\n// Cada palavra vira um vetor de dimensão 768\nembeddings = [\n  [0.2, -0.1, 0.5, ...], // \"o\"\n  [0.1, 0.3, -0.2, ...], // \"gato\"\n  [0.4, 0.1, 0.0, ...],  // \"subiu\"\n]\n```\n\nTipo 3: Positional Encoding (adicionar \"posição\" ao vetor)\n```php\n// Porque o modelo precisa saber que \"o\" vem primeiro\n// Adiciona um vetor de posição a cada embedding\nposition_encoding = [\n  sin(0/10000^(2i/768)), cos(0/10000^(2i/768)), ..., // posição 0\n  sin(1/10000^(2i/768)), cos(1/10000^(2i/768)), ..., // posição 1\n  ...\n]\n\n// Agora cada palavra tem \"posição embutida\"\nembedded_with_position = embeddings + position_encoding\n```\n\n## Tipo 4: Attention (O Coração)\n\nAttention responde a pergunta:\n\"Quando processo a palavra 'subiu', com qual outra palavra devo pagar atenção?\"\n\n**Resposta:** Com \"gato\" (quem subiu) e \"telhado\" (aonde foi).\n\nMas o modelo não sabe disso. Ele aprende com números.\n\n```php\n// Você tem 3 matrizes que o modelo aprendeu:\n// Query (Q), Key (K), Value (V)\n// Todas com dimensão [seq_len, d_k] onde d_k = 64\n\nQ = query_matrix @ embeddings;  // \"O que tô procurando?\"\nK = key_matrix @ embeddings;    // \"Chaves para encontrar\"\nV = value_matrix @ embeddings;  // \"Valores pra retornar\"\n\n// Attention score = quanto uma palavra interessa a outra\nscores = Q @ K.T / sqrt(d_k)  // matrix multiply\n// Forma matriz [seq_len, seq_len]\n// scores[0][1] = \"quanto 'o' liga para 'gato'\"\n\n// Softmax pra virar probabilidade\nattention_weights = softmax(scores)  // Cada linha soma 1\n// Agora é \"proporção de atenção\"\n\n// Output = weighted sum dos valores\noutput = attention_weights @ V\n// Cada palavra recebe combinação de todas outras palavras\n```\n\n**Exemplo concreto:**\n\n```\nFrase: [\"o\", \"gato\", \"subiu\"]\n\nQuando processamos \"subiu\":\n  Query[\"subiu\"] vs Key[\"o\"] = 0.1 (pouca relevância)\n  Query[\"subiu\"] vs Key[\"gato\"] = 0.7 (alta relevância)\n  Query[\"subiu\"] vs Key[\"subiu\"] = 0.2 (médio)\n  \nSoftmax: [0.15, 0.65, 0.20]  (normalizados pra 1)\n\nOutput[\"subiu\"] = 0.15 * Value[\"o\"] + 0.65 * Value[\"gato\"] + 0.20 * Value[\"subiu\"]\n                = Vetor que mistura principalmente \"gato\"\n```\n\nVocê viu? **Attention é só matrix multiplication + softmax**.\n\n## Tipo 5: Multi-Head Attention\n\nUm cabeça de attention não é suficiente. Você quer múltiplas \"perspectivas\".\n\n```php\n// Head 1: Foca em relações gramaticais\nattention_head_1 = attention(Q1, K1, V1)\n// \"o gato\" = nome + artigo\n\n// Head 2: Foca em relações semânticas\nattention_head_2 = attention(Q2, K2, V2)\n// \"gato subiu\" = agente + verbo\n\n// Head 3: Foca em posição\nattention_head_3 = attention(Q3, K3, V3)\n// Mede distância entre palavras\n\n// Concatena todos\noutput = concat(head_1, head_2, head_3)\noutput = linear_layer @ output  // Mistura as perspectivas\n```\n\nGPT usa 12 cabeças. BERT usa 12. Claude usa 128.\n\nCada cabeça aprendeu especializar em algo diferente.\n\n## Tipo 6: Feed-Forward Network\n\nDepois de attention, cada palavra passa por uma rede neural simples.\n\n```php\n// Aplicado a cada posição independentemente\noutput = ReLU(linear1 @ input) @ linear2\n// Camada: [768 → 3072 → 768]\n\n// ReLU = max(0, x)\n// Introduz não-linearidade\n// Permite que o modelo aprenda padrões complexos\n```\n\n## Tipo 7: Normalization + Residual\n\nEstranheza matemática que torna tudo funcionar:\n\n```php\n// Residual connection (pula uma camada)\noutput = layer_norm(input + attention(input))\noutput = layer_norm(output + feed_forward(output))\n\n// Layer norm = normaliza cada amostra pra média 0, std 1\n// Estabiliza treinamento\n// Sem isso, treinamento diverge\n```\n\n## Stack Completo\n\n```\nInput: \"o gato subiu no telhado\"\n  ↓\n[1] Tokenize → [521, 3412, 8821, 4521, 9234]\n  ↓\n[2] Embed + Position → 5 vetores [768]\n  ↓\n[3] Multi-Head Attention (12 cabeças)\n  ├─ cada cabeça: Q @ K^T / √d_k → softmax → @ V\n  └─ concat + linear → [768]\n  ↓\n[4] Add & Norm\n  ↓\n[5] Feed-Forward\n  ├─ linear [768→3072]\n  ├─ ReLU\n  └─ linear [3072→768]\n  ↓\n[6] Add & Norm\n  ↓\n[Repeat steps 3-6 for each of 12 layers]\n  ↓\n[7] Output: 5 vetores [768] representando cada palavra com contexto\n  ↓\n[8] Linear layer [768 → vocab_size]\n  ↓\n[9] Softmax → distribuição sobre próxima palavra\n```\n\n## Implementei em PHP (200 Linhas)\n\n```php\nclass Transformer {\n  public $W_q, $W_k, $W_v;  // Matrizes de atenção\n  \n  public function attention($Q, $K, $V) {\n    $scores = $Q @ $K->transpose();\n    $scores = $scores * (1.0 / sqrt(64));\n    \n    $weights = softmax($scores);\n    return $weights @ $V;\n  }\n  \n  public function forward($input) {\n    $x = $this->embed($input);\n    $x = $x + $this->positional_encoding($input);\n    \n    foreach (range(0, 11) as $layer) {\n      // Multi-head attention\n      $attn_out = $this->multi_head_attention($x);\n      $x = layer_norm($x + $attn_out);\n      \n      // Feed forward\n      $ff_out = $this->feed_forward($x);\n      $x = layer_norm($x + $ff_out);\n    }\n    \n    return $x;\n  }\n}\n```\n\n## O Insight Real: Por Que Funciona\n\nTransformers funcionam porque **cada palavra pode \"ver\" todas outras palavras ao mesmo tempo**.\n\nIsso permite:\n1. **Paralelização** - processa tudo de uma vez\n2. **Contexto global** - cada palavra sabe tudo que veio antes e depois\n3. **Aprendizado de relações** - attention aprende quais palavras importam\n4. **Flexibilidade** - mesma arquitetura funciona pra qualquer comprimento\n\nA matemática é simples (matrix multiply, softmax), mas as combinações são poderosas.\n\n## Trade-off: Complexidade vs Poder\n\nTransformers não são perfeitos:\n\n```\n✓ Paralelizável\n✗ Attention tem custo O(n²) em memória\n  → Com 2048 tokens, matriz é 2048x2048\n  → Explosion de memória\n\n✓ Aprende relações de longo prazo\n✗ Tá sempre errado sobre \"qual ordem os números vêm\"\n  → Precisa de position encoding especial\n\n✓ Funciona bem em prática\n✗ Ninguém entende bem por quê\n  → Interpretabilidade é open problem\n```\n\n## Resumo: Da Matemática Pro Produto\n\nVocê tem 3 coisas essenciais:\n\n1. **Embedding**: Converter texto em números\n2. **Attention**: Descobrir quais palavras importam uma pra outra\n3. **Feed-Forward**: Processar cada palavra com rede neural\n\nRepete 12+ vezes, adia alguns tensores, e você tem GPT.\n\nNão é magia. É matemática linda.",
        en: "## Why Transformers Exist\n\nBefore transformers, neural networks for language used **recurrence** (RNNs, LSTMs).\n\nProblem: you process word by word, sequentially.\n\n```\nInput: \"The cat climbed on the roof\"\n\nRNN:\n  Step 1: Process \"The\"\n  Step 2: Process \"cat\" (with memory of \"The\")\n  Step 3: Process \"climbed\" (with memory of \"The\" + \"cat\")\n  ... sequential, slow\n```\n\nThat's slow in production and **impossible to parallelize**. GPU loves processing everything at once. Sequential RNNs = angry GPU.\n\n**Transformers came to fix this:**\n\nProcess all words **at the same time**, using attention to link each word with all others.\n\n## The Insight: Attention Is Just Basic Math\n\nYou have a sentence:\n```\nsentence = [\"the\", \"cat\", \"climbed\", \"on\", \"roof\"]\n```\n\nStep 1: Tokenize\n```php\ntoken_ids = [521, 3412, 8821, 4521, 9234]  // Vocabulary IDs\n```\n\nStep 2: Embedding (transform numbers into dense vectors)\n```php\n// Each word becomes a vector of dimension 768\nembeddings = [\n  [0.2, -0.1, 0.5, ...], // \"the\"\n  [0.1, 0.3, -0.2, ...], // \"cat\"\n  [0.4, 0.1, 0.0, ...],  // \"climbed\"\n]\n```\n\nStep 3: Positional Encoding (add \"position\" to vector)\n```php\n// Because model needs to know \"the\" comes first\n// Add position vector to each embedding\nposition_encoding = [\n  sin(0/10000^(2i/768)), cos(0/10000^(2i/768)), ..., // position 0\n  sin(1/10000^(2i/768)), cos(1/10000^(2i/768)), ..., // position 1\n  ...\n]\n\n// Now each word has \"position embedded\"\nembedded_with_position = embeddings + position_encoding\n```\n\n## Step 4: Attention (The Heart)\n\nAttention answers the question:\n\"When I process the word 'climbed', which other word should I pay attention to?\"\n\n**Answer:** To \"cat\" (who climbed) and \"roof\" (where it went).\n\nBut the model doesn't know this. It learns with numbers.\n\n```php\n// You have 3 matrices the model learned:\n// Query (Q), Key (K), Value (V)\n// All with dimension [seq_len, d_k] where d_k = 64\n\nQ = query_matrix @ embeddings;  // \"What am I looking for?\"\nK = key_matrix @ embeddings;    // \"Keys to find it\"\nV = value_matrix @ embeddings;  // \"Values to return\"\n\n// Attention score = how much one word matters to another\nscores = Q @ K.T / sqrt(d_k)  // matrix multiply\n// Forms matrix [seq_len, seq_len]\n// scores[0][1] = \"how much does 'the' matter to 'cat'\"\n\n// Softmax to turn into probability\nattention_weights = softmax(scores)  // Each row sums to 1\n// Now it's \"proportion of attention\"\n\n// Output = weighted sum of values\noutput = attention_weights @ V\n// Each word receives combination of all other words\n```\n\n**Concrete example:**\n\n```\nSentence: [\"the\", \"cat\", \"climbed\"]\n\nWhen we process \"climbed\":\n  Query[\"climbed\"] vs Key[\"the\"] = 0.1 (low relevance)\n  Query[\"climbed\"] vs Key[\"cat\"] = 0.7 (high relevance)\n  Query[\"climbed\"] vs Key[\"climbed\"] = 0.2 (medium)\n  \nSoftmax: [0.15, 0.65, 0.20]  (normalized to 1)\n\nOutput[\"climbed\"] = 0.15 * Value[\"the\"] + 0.65 * Value[\"cat\"] + 0.20 * Value[\"climbed\"]\n                   = Vector mainly mixing \"cat\"\n```\n\nYou see? **Attention is just matrix multiplication + softmax**.\n\n## Step 5: Multi-Head Attention\n\nOne attention head isn't enough. You want multiple \"perspectives\".\n\n```php\n// Head 1: Focuses on grammatical relations\nattention_head_1 = attention(Q1, K1, V1)\n// \"the cat\" = article + noun\n\n// Head 2: Focuses on semantic relations\nattention_head_2 = attention(Q2, K2, V2)\n// \"cat climbed\" = agent + verb\n\n// Head 3: Focuses on position\nattention_head_3 = attention(Q3, K3, V3)\n// Measures distance between words\n\n// Concatenate all\noutput = concat(head_1, head_2, head_3)\noutput = linear_layer @ output  // Mix perspectives\n```\n\nGPT uses 12 heads. BERT uses 12. Claude uses 128.\n\nEach head learned to specialize in something different.\n\n## Step 6: Feed-Forward Network\n\nAfter attention, each word goes through a simple neural network.\n\n```php\n// Applied to each position independently\noutput = ReLU(linear1 @ input) @ linear2\n// Layer: [768 → 3072 → 768]\n\n// ReLU = max(0, x)\n// Introduces non-linearity\n// Allows model to learn complex patterns\n```\n\n## Step 7: Normalization + Residual\n\nMathematical weirdness that makes everything work:\n\n```php\n// Residual connection (skip a layer)\noutput = layer_norm(input + attention(input))\noutput = layer_norm(output + feed_forward(output))\n\n// Layer norm = normalize each sample to mean 0, std 1\n// Stabilizes training\n// Without it, training diverges\n```\n\n## Complete Stack\n\n```\nInput: \"the cat climbed on the roof\"\n  ↓\n[1] Tokenize → [521, 3412, 8821, 4521, 9234]\n  ↓\n[2] Embed + Position → 5 vectors [768]\n  ↓\n[3] Multi-Head Attention (12 heads)\n  ├─ each head: Q @ K^T / √d_k → softmax → @ V\n  └─ concat + linear → [768]\n  ↓\n[4] Add & Norm\n  ↓\n[5] Feed-Forward\n  ├─ linear [768→3072]\n  ├─ ReLU\n  └─ linear [3072→768]\n  ↓\n[6] Add & Norm\n  ↓\n[Repeat steps 3-6 for each of 12 layers]\n  ↓\n[7] Output: 5 vectors [768] representing each word with context\n  ↓\n[8] Linear layer [768 → vocab_size]\n  ↓\n[9] Softmax → distribution over next word\n```\n\n## I Implemented It in PHP (200 Lines)\n\n```php\nclass Transformer {\n  public $W_q, $W_k, $W_v;  // Attention matrices\n  \n  public function attention($Q, $K, $V) {\n    $scores = $Q @ $K->transpose();\n    $scores = $scores * (1.0 / sqrt(64));\n    \n    $weights = softmax($scores);\n    return $weights @ $V;\n  }\n  \n  public function forward($input) {\n    $x = $this->embed($input);\n    $x = $x + $this->positional_encoding($input);\n    \n    foreach (range(0, 11) as $layer) {\n      // Multi-head attention\n      $attn_out = $this->multi_head_attention($x);\n      $x = layer_norm($x + $attn_out);\n      \n      // Feed forward\n      $ff_out = $this->feed_forward($x);\n      $x = layer_norm($x + $ff_out);\n    }\n    \n    return $x;\n  }\n}\n```\n\n## The Real Insight: Why It Works\n\nTransformers work because **each word can \"see\" all other words at the same time**.\n\nThis enables:\n1. **Parallelization** - processes everything at once\n2. **Global context** - each word knows everything before and after\n3. **Learning relationships** - attention learns which words matter\n4. **Flexibility** - same architecture works for any length\n\nThe math is simple (matrix multiply, softmax), but combinations are powerful.\n\n## Trade-off: Complexity vs Power\n\nTransformers aren't perfect:\n\n```\n✓ Parallelizable\n✗ Attention costs O(n²) memory\n  → With 2048 tokens, matrix is 2048x2048\n  → Memory explosion\n\n✓ Learns long-term relationships\n✗ Always wrong about \"which order numbers come\"\n  → Needs special position encoding\n\n✓ Works well in practice\n✗ No one really understands why\n  → Interpretability is open problem\n```\n\n## Summary: From Math to Product\n\nYou have 3 essential things:\n\n1. **Embedding**: Convert text to numbers\n2. **Attention**: Discover which words matter to each other\n3. **Feed-Forward**: Process each word with neural network\n\nRepeat 12+ times, skip some tensors, and you have GPT.\n\nIt's not magic. It's beautiful math.",
      },
    },
    {
      slug: "laravel-tenancy",
      title: {
        pt: "Multi-tenancy no Laravel: quatro formas, uma armadilha",
        en: "Multi-tenancy in Laravel: four shapes, one trap",
      },
      date: "2025-11-11",
      read: "15 min",
      kicker: { pt: "Arquitetura", en: "Architecture" },
      summary: {
        pt: "Comece com row-scoped simples. Migre para schema per tenant quando escalar. Database per tenant se compliance exigir.",
        en: "Start with simple row-scoped. Migrate to schema per tenant when scaling. Database per tenant if compliance requires.",
      },
      excerpt: {
        pt: "## Por Que Multi-Tenancy?\n\nVocê construiu um SaaS. Agora tem 100 clientes.\n\nOpção A: Um banco de dados por cliente\n→ Database gigante com 100 schemas idênticos\n→ Impossível escalar\n\nOpção B: Um único banco com tudo misturado\n→ Rápido de começar\n→ Fácil de quebrar (um SELECT sem WHERE e expõe todos)\n\n**Multi-tenancy** é a solução: Um ou poucos bancos, mas **dados isolados logicamente**.\n\n## Estratégia 1: Database Per Tenant\n\n**Estrutura:**\n```\nacme_db: {users, orders, products}\ncompany_x_db: {users, orders, products}\ncompany_y_db: {users, orders, products}\n```\n\n**Implementação:**\n```php\nDB::connection(\"tenant_{$tenant->id}\")->table('users')->get();\n```\n\n**Vantagens:**\n• Isolamento máximo (cliente nunca vê dados de outro)\n• Backup independente (perde um, outros sobrevivem)\n• Scaling fácil (mete cada cliente em servidor diferente)\n• Compliance fácil (alguns países exigem dados separados)\n\n**Desvantagens:**\n• Complexidade operacional (100 bancos = 100x mais trabalho)\n• Migration é hell (rodar migrations em 100 bancos é trabalhoso)\n• Sharing de dados entre tenants é impossível\n• Custo: cada database = custo de infraestrutura\n• Backups: 100 bancos = 100x mais storage\n\n**Quando usar:**\n→ Poucos clientes (< 10)\n→ Clientes enterprise que pagam bastante\n→ Compliance requerindo segregação extrema\n\n## Estratégia 2: Schema Per Tenant (A Melhor)\n\n**Estrutura:**\n```sql\nshared_db.public_schema:\n  users (apenas logins globais)\n  tenants (lista de quem é cliente)\n  \nshared_db.tenant_acme_schema:\n  users (usuários da Acme)\n  orders (pedidos da Acme)\n  products (produtos da Acme)\n  \nshared_db.tenant_company_x_schema:\n  users (usuários da Company X)\n  orders (pedidos da Company X)\n  products (produtos da Company X)\n```\n\n**Implementação:**\n```php\n// Middleware: Detecta tenant do request\nPublic::middleware(function ($request, $next) {\n  $tenant = Tenant::whereSubdomain(request()->host())->first();\n  // \"acme.app.com\" → tenant_id = 1\n  \n  DB::statement(\"SET search_path = tenant_{$tenant->id}, public\");\n  // PostgreSQL: procura em tenant_acme_schema primeiro\n  \n  // Agora todos os queries usam o schema correto\n  $users = User::all();  // SELECT from tenant_acme.users\n  \n  return $next($request);\n});\n```\n\n**Vantagens:**\n• Melhor ROI (um banco, muitos schemas)\n• Migrations rodadas uma vez (muito mais rápido)\n• Compartilhamento de dados fácil\n• Escalável até 1.000+ clientes\n• Backup único com multiplos schemas\n\n**Desvantagens:**\n• Um schema quebra = todos no mesmo banco sofrem\n• Queries entre schemas é complicado\n• Compliance: alguns reguladores não aceitam (dados no mesmo hardware)\n\n**Quando usar:**\n→ SaaS escalável (50-1000 clientes)\n→ Você quer simplicidade operacional\n→ Clientes aceitam compartilhar hardware\n\n## Estratégia 3: Row-Scoped (A Armadilha)\n\n**Estrutura:**\n```sql\nTable: users\n┌────┬─────────────┬───────────────┐\n│ id │ tenant_id   │ email         │\n├────┼─────────────┼───────────────┤\n│ 1  │ acme        │ alice@acme    │\n│ 2  │ company_x   │ bob@company   │\n│ 3  │ acme        │ charlie@acme  │\n└────┴─────────────┴───────────────┘\n```\n\n**Implementação:**\n```php\nUser::where('tenant_id', $tenant->id)->get();\n```\n\n**Vantagens:**\n• Super simples de começar\n• Um banco, uma tabela, um índice\n• Fácil fazer queries entre tenants (se precisar)\n\n**Desvantagens (A Armadilha):**\n• **SELECT * FROM users sem WHERE = BUG CRÍTICO**\n  Se esquecer tenant_id em um lugar, vazou dados pra todos\n\n• **Performance**: Sem índice em tenant_id, é O(n) sempre\n  ```\n  Bad: SELECT * FROM users WHERE email = 'x'\n  → Full table scan por 10K usuários\n  \n  Good: SELECT * FROM users WHERE tenant_id = 'acme' AND email = 'x'\n  → Index range scan\n  ```\n  \n  Mas se esquecer tenant_id no index, é slow.\n\n• **Global scope pollution**: Você escreve\n  ```php\n  User::find(5);\n  ```\n  \n  Retorna qual tenant? O 5º usuário de qual cliente?\n  Sem context, isso é ambíguo.\n\n**O Disaster:**\n```php\n// Controller\npublic function updateUser($id) {\n  $user = User::find($id);  // ← BUG: Qual tenant?\n  $user->update(request()->all());\n  // Se ID 5 pertence a outro cliente, você editou conta deles\n}\n\n// Implementação segura:\npublic function updateUser($id) {\n  $user = User::where('tenant_id', Auth::user()->tenant_id)\n    ->findOrFail($id);\n  // Agora só retorna se pertence ao tenant do usuário\n  $user->update(request()->all());\n}\n```\n\n**Quando usar:**\n→ MVP rápido (você sabe os riscos)\n→ Time pequeno, confiável\n→ Depois você migra pra Schema per Tenant\n\n**Quando NÃO usar:**\n→ Clientes pagando bastante\n→ Sensibilidade em data privacy\n→ Team grande (fácil alguém esquecer tenant_id)\n\n## Estratégia 4: Column-Scoped (Nunca Use)\n\n**Estrutura:**\n```sql\nTable: users\n┌────┬──────────┬─────────────────────────┐\n│ id │ email    │ tenant_access           │\n├────┼──────────┼─────────────────────────┤\n│ 1  │ alice    │ {acme, company_x}       │  ← Array de tenants\n│ 2  │ bob      │ {acme}                  │\n└────┴──────────┴─────────────────────────┘\n```\n\n**Problema:**\nVocê nunca consegue saber \"quais dados vê este usuário\". Cada query vira:\n\n```php\nUser::whereJsonContains('tenant_access', $tenant->id)->get();\n// JSON contains check = sempre slow\n// Index não funciona bem\n```\n\n**Resultado:** Performance horrível, security difícil.\n\n**Nunca use isso.**\n\n## Decisão: Qual Escolher?\n\n**Comece com:** Row-Scoped (simples, rápido)\n**Cuidado:** Adicione global scope middleware\n**Quando escalar:** Migre pra Schema per Tenant\n**Se compliance:** Database per Tenant\n\n## Implementação Segura: Global Scope\n\n```php\n// app/Models/Traits/BelongsToTenant.php\ntrait BelongsToTenant\n{\n  protected static function booted(): void\n  {\n    // Adiciona automaticamente WHERE tenant_id = X em TODO query\n    static::addGlobalScope(new TenantScope);\n  }\n}\n\n// app/Scopes/TenantScope.php\nclass TenantScope implements Scope\n{\n  public function apply(Builder $builder, Model $model): void\n  {\n    if (auth()->check()) {\n      $builder->where('tenant_id', auth()->user()->tenant_id);\n    }\n  }\n}\n\n// app/Models/User.php\nclass User extends Model\n{\n  use BelongsToTenant;  // ← Agora TODA query filtra automaticamente\n}\n\n// Uso:\nUser::find(5);  // Automático: WHERE tenant_id = 'acme' AND id = 5\nUser::all();    // Automático: WHERE tenant_id = 'acme'\n```\n\n**Segurança**: Mesmo se esquecer tenant_id, o scope filtra.\n\n## Migrando de Row-Scoped → Schema per Tenant\n\n**Dia 1:** Você tem\n```sql\nusers: [id, email, tenant_id]\norders: [id, user_id, tenant_id]\n```\n\n**Dia 2:** Você cria novo schema\n```bash\npsql -d shared_db -c \"CREATE SCHEMA tenant_acme\"\npsql -d shared_db -c \"SET search_path = tenant_acme; CREATE TABLE users AS SELECT * FROM public.users WHERE tenant_id = 'acme'\"\n```\n\n**Dia 3:** Migração final\n```php\nclass MigrateToSchemaPerTenant {\n  public function handle() {\n    foreach (Tenant::all() as $tenant) {\n      // Cria schema\n      DB::statement(\"CREATE SCHEMA tenant_{$tenant->id}\");\n      \n      // Copia dados\n      DB::statement(\n        \"CREATE TABLE tenant_{$tenant->id}.users AS \n         SELECT * FROM public.users WHERE tenant_id = ?\",\n        [$tenant->id]\n      );\n      \n      // Remove coluna tenant_id (agora é redundante, está no schema)\n      DB::statement(\n        \"ALTER TABLE tenant_{$tenant->id}.users \n         DROP COLUMN tenant_id\"\n      );\n    }\n    \n    // Depois\n    DB::statement(\"DROP TABLE public.users\");\n  }\n}\n```\n\n## Caso Real: Trabalho Amigo\n\nUsamos:\n**Row-Scoped com Global Scope (MVP)**\n↓ (depois de 50 usuários)\n**Schema per Tenant**\n↓ (se escalar para 10K)\n**Database per Tenant (alguns clientes enterprise)**\n\n**Resultado:**\n- MVP em 2 semanas (row-scoped)\n- Scaling transparente pra 5K usuários (schema per tenant)\n- Enterprise customers felizes (seus próprios bancos)\n\n## Checklist: Sua Multi-Tenancy é Segura?\n\n**Sim se:**\n• Toda Model usa BelongsToTenant trait\n• Global scope filtra automaticamente\n• Você roda queries com logged-in user apenas\n• Índices em tenant_id + (other columns)\n• Migrations com scaffold de teste\n\n**Não se:**\n• Queries sem WHERE tenant_id\n• Você confia que devs sempre lembram tenant_id\n• Sem global scope\n• Sem testes verificando isolamento\n\n## Resumo\n\n**Row-Scoped:** Rápido pra começar, perigoso sem discipline\n**Schema per Tenant:** Melhor ROI, seguro por arquitetura\n**Database per Tenant:** Isolamento máximo, operacional hell\n**Column-Scoped:** Não use\n\nComece simples, migre quando precisar escalar.",
        en: "## Why Multi-Tenancy?\n\nYou built a SaaS. Now you have 100 customers.\n\nOption A: One database per customer\n→ Massive database with 100 identical schemas\n→ Impossible to scale\n\nOption B: One database with everything mixed\n→ Quick to start\n→ Easy to break (one SELECT without WHERE and expose everyone)\n\n**Multi-tenancy** is the solution: One or few databases, but **data isolated logically**.\n\n## Strategy 1: Database Per Tenant\n\n**Structure:**\n```\nacme_db: {users, orders, products}\ncompany_x_db: {users, orders, products}\ncompany_y_db: {users, orders, products}\n```\n\n**Implementation:**\n```php\nDB::connection(\"tenant_{$tenant->id}\")->table('users')->get();\n```\n\n**Advantages:**\n• Maximum isolation (customer never sees another's data)\n• Independent backups (lose one, others survive)\n• Scaling easy (put each customer on different server)\n• Compliance easy (some countries require data separation)\n\n**Disadvantages:**\n• Operational complexity (100 databases = 100x more work)\n• Migration is hell (running migrations on 100 databases is painful)\n• Data sharing between tenants is impossible\n• Cost: each database = infrastructure cost\n• Backups: 100 databases = 100x more storage\n\n**When to use:**\n→ Few customers (< 10)\n→ Enterprise customers paying big\n→ Compliance requiring extreme segregation\n\n## Strategy 2: Schema Per Tenant (The Best)\n\n**Structure:**\n```sql\nshared_db.public_schema:\n  users (global logins only)\n  tenants (who is a customer)\n  \nshared_db.tenant_acme_schema:\n  users (Acme's users)\n  orders (Acme's orders)\n  products (Acme's products)\n  \nshared_db.tenant_company_x_schema:\n  users (Company X's users)\n  orders (Company X's orders)\n  products (Company X's products)\n```\n\n**Implementation:**\n```php\n// Middleware: Detects tenant from request\nPublic::middleware(function ($request, $next) {\n  $tenant = Tenant::whereSubdomain(request()->host())->first();\n  // \"acme.app.com\" → tenant_id = 1\n  \n  DB::statement(\"SET search_path = tenant_{$tenant->id}, public\");\n  // PostgreSQL: searches in tenant_acme_schema first\n  \n  // Now all queries use correct schema\n  $users = User::all();  // SELECT from tenant_acme.users\n  \n  return $next($request);\n});\n```\n\n**Advantages:**\n• Better ROI (one database, many schemas)\n• Migrations run once (much faster)\n• Data sharing easy\n• Scales to 1,000+ customers\n• Single backup with multiple schemas\n\n**Disadvantages:**\n• One schema breaks = everyone in same database suffers\n• Cross-schema queries are complicated\n• Compliance: some regulators won't accept (data on same hardware)\n\n**When to use:**\n→ Scalable SaaS (50-1000 customers)\n→ You want operational simplicity\n→ Customers accept shared hardware\n\n## Strategy 3: Row-Scoped (The Trap)\n\n**Structure:**\n```sql\nTable: users\n┌────┬─────────────┬───────────────┐\n│ id │ tenant_id   │ email         │\n├────┼─────────────┼───────────────┤\n│ 1  │ acme        │ alice@acme    │\n│ 2  │ company_x   │ bob@company   │\n│ 3  │ acme        │ charlie@acme  │\n└────┴─────────────┴───────────────┘\n```\n\n**Implementation:**\n```php\nUser::where('tenant_id', $tenant->id)->get();\n```\n\n**Advantages:**\n• Super simple to start\n• One database, one table, one index\n• Easy to query across tenants (if needed)\n\n**Disadvantages (The Trap):**\n• **SELECT * FROM users without WHERE = CRITICAL BUG**\n  Forget tenant_id in one place, and you've leaked data to everyone\n\n• **Performance**: Without index on tenant_id, it's O(n) always\n  ```\n  Bad: SELECT * FROM users WHERE email = 'x'\n  → Full table scan across 10K users\n  \n  Good: SELECT * FROM users WHERE tenant_id = 'acme' AND email = 'x'\n  → Index range scan\n  ```\n  \n  But if you forget tenant_id in index, it's slow.\n\n• **Global scope pollution**: You write\n  ```php\n  User::find(5);\n  ```\n  \n  Returns which tenant's user? User 5 from which customer?\n  Without context, ambiguous.\n\n**The Disaster:**\n```php\n// Controller\npublic function updateUser($id) {\n  $user = User::find($id);  // ← BUG: Which tenant?\n  $user->update(request()->all());\n  // If ID 5 belongs to another customer, you edited their account\n}\n\n// Safe implementation:\npublic function updateUser($id) {\n  $user = User::where('tenant_id', Auth::user()->tenant_id)\n    ->findOrFail($id);\n  // Now returns only if owned by logged-in user's tenant\n  $user->update(request()->all());\n}\n```\n\n**When to use:**\n→ Fast MVP (you know the risks)\n→ Small, reliable team\n→ Later you migrate to Schema per Tenant\n\n**When NOT to use:**\n→ Customers paying big\n→ Data privacy sensitive\n→ Large team (easy for someone to forget tenant_id)\n\n## Strategy 4: Column-Scoped (Never Use)\n\n**Structure:**\n```sql\nTable: users\n┌────┬──────────┬─────────────────────────┐\n│ id │ email    │ tenant_access           │\n├────┼──────────┼─────────────────────────┤\n│ 1  │ alice    │ {acme, company_x}       │  ← Array of tenants\n│ 2  │ bob      │ {acme}                  │\n└────┴──────────┴─────────────────────────┘\n```\n\n**Problem:**\nYou never know \"what data does this user see\". Every query becomes:\n\n```php\nUser::whereJsonContains('tenant_access', $tenant->id)->get();\n// JSON contains check = always slow\n// Index doesn't work well\n```\n\n**Result:** Terrible performance, hard security.\n\n**Never use this.**\n\n## Decision: Which One?\n\n**Start with:** Row-Scoped (simple, fast)\n**Be careful:** Add global scope middleware\n**When scaling:** Migrate to Schema per Tenant\n**If compliance:** Database per Tenant\n\n## Safe Implementation: Global Scope\n\n```php\n// app/Models/Traits/BelongsToTenant.php\ntrait BelongsToTenant\n{\n  protected static function booted(): void\n  {\n    // Automatically add WHERE tenant_id = X to EVERY query\n    static::addGlobalScope(new TenantScope);\n  }\n}\n\n// app/Scopes/TenantScope.php\nclass TenantScope implements Scope\n{\n  public function apply(Builder $builder, Model $model): void\n  {\n    if (auth()->check()) {\n      $builder->where('tenant_id', auth()->user()->tenant_id);\n    }\n  }\n}\n\n// app/Models/User.php\nclass User extends Model\n{\n  use BelongsToTenant;  // ← Now EVERY query filters automatically\n}\n\n// Usage:\nUser::find(5);  // Automatic: WHERE tenant_id = 'acme' AND id = 5\nUser::all();    // Automatic: WHERE tenant_id = 'acme'\n```\n\n**Security**: Even if you forget tenant_id, scope filters.\n\n## Migrating Row-Scoped → Schema per Tenant\n\n**Day 1:** You have\n```sql\nusers: [id, email, tenant_id]\norders: [id, user_id, tenant_id]\n```\n\n**Day 2:** You create new schema\n```bash\npsql -d shared_db -c \"CREATE SCHEMA tenant_acme\"\npsql -d shared_db -c \"SET search_path = tenant_acme; CREATE TABLE users AS SELECT * FROM public.users WHERE tenant_id = 'acme'\"\n```\n\n**Day 3:** Final migration\n```php\nclass MigrateToSchemaPerTenant {\n  public function handle() {\n    foreach (Tenant::all() as $tenant) {\n      // Create schema\n      DB::statement(\"CREATE SCHEMA tenant_{$tenant->id}\");\n      \n      // Copy data\n      DB::statement(\n        \"CREATE TABLE tenant_{$tenant->id}.users AS \n         SELECT * FROM public.users WHERE tenant_id = ?\",\n        [$tenant->id]\n      );\n      \n      // Remove tenant_id column (now redundant, in schema)\n      DB::statement(\n        \"ALTER TABLE tenant_{$tenant->id}.users \n         DROP COLUMN tenant_id\"\n      );\n    }\n    \n    // After\n    DB::statement(\"DROP TABLE public.users\");\n  }\n}\n```\n\n## Real Case: Trabalho Amigo\n\nWe use:\n**Row-Scoped with Global Scope (MVP)**\n↓ (after 50 users)\n**Schema per Tenant**\n↓ (if scaling to 10K)\n**Database per Tenant (some enterprise customers)**\n\n**Result:**\n- MVP in 2 weeks (row-scoped)\n- Transparent scaling to 5K users (schema per tenant)\n- Happy enterprise customers (their own databases)\n\n## Checklist: Is Your Multi-Tenancy Safe?\n\n**Yes if:**\n• Every Model uses BelongsToTenant trait\n• Global scope filters automatically\n• You run queries with logged-in user only\n• Indexes on tenant_id + (other columns)\n• Migrations with test scaffolding\n\n**No if:**\n• Queries without WHERE tenant_id\n• You trust devs to always remember tenant_id\n• No global scope\n• No tests verifying isolation\n\n## Summary\n\n**Row-Scoped:** Quick to start, risky without discipline\n**Schema per Tenant:** Best ROI, safe by architecture\n**Database per Tenant:** Maximum isolation, operational nightmare\n**Column-Scoped:** Don't use\n\nStart simple, migrate when you need to scale.",
      },
    },
    {
      slug: "claude-design",
      title: {
        pt: "O design de Claude: como Anthropic construiu uma IA alinhada",
        en: "Claude's design: how Anthropic built aligned AI",
      },
      date: "2026-04-15",
      read: "9 min",
      kicker: { pt: "IA", en: "AI" },
      summary: {
        pt: "Constitutional AI, RLHF honesto, padrões de recusa. Construir IA confiável requer design firme, não só escala.",
        en: "Constitutional AI, honest RLHF, refusal patterns. Building trustworthy AI requires firm design, not just scale.",
      },
      excerpt: {
        pt: "Claude não é só um modelo maior. É uma arquitetura deliberada: Constitutional AI, RLHF honesto, rejeição de certos padrões de uso. O trade-off: menos compliance games, mais transparência. Construir IA que você consegue confiar requer design firme — não é emergência de escala.",
        en: "Claude isn't just a bigger model. It's deliberate architecture: Constitutional AI, honest RLHF, refusal patterns. The trade-off: fewer compliance games, more transparency. Building AI you can trust requires firm design — it's not scale emergence.",
      },
    },
    {
      slug: "opus-4-7-capabilities",
      title: {
        pt: "Claude Opus 4.7: raciocínio profundo em tempo real",
        en: "Claude Opus 4.7: deep reasoning in real time",
      },
      date: "2026-04-14",
      read: "6 min",
      kicker: { pt: "Ferramentas", en: "Tools" },
      summary: {
        pt: "Melhorias em raciocínio complexo, contexto, speed. Viável para code generation, análise técnica, design de sistemas.",
        en: "Improvements in complex reasoning, context, inference speed. Viable for code generation, technical analysis, system design.",
      },
      excerpt: {
        pt: "Opus 4.7 traz melhorias em raciocínio complexo, interpretação de contexto e velocidade de inference. Prático para: code generation com garantias, análise técnica profunda, architecting de sistemas. Curva de custo vs. output é agora viável para muitos use-cases que antes exigiam serviços separados.",
        en: "Opus 4.7 brings improvements in complex reasoning, context interpretation, and inference speed. Practical for: code generation with guarantees, deep technical analysis, system architecting. Cost vs. output curve is now viable for many use cases that previously required separate services.",
      },
    },
    {
      slug: "ai-in-software-dev",
      title: {
        pt: "IA no desenvolvimento: não é automação, é amplificação",
        en: "AI in software development: it's not automation, it's amplification",
      },
      date: "2026-04-13",
      read: "8 min",
      kicker: { pt: "Pensamentos", en: "Thoughts" },
      summary: {
        pt: "LLMs aceleram boilerplate. Arquitetura, review, julgamento ainda são seus. Valor real: 10x mais rápido explorando.",
        en: "LLMs accelerate boilerplate. Architecture, review, judgment are still yours. Real value: 10x faster exploring.",
      },
      excerpt: {
        pt: "A ilusão: LLMs escrevem código, você coloca em produção. A realidade: LLMs aceleram o boilerplate, mas decisões arquiteturais, review crítico, e julgamento ainda são seus. O valor real é velocidade em exploração e prototipagem. Você consegue iterar 10x mais rápido, desde que você saiba o que está iterando.",
        en: "The illusion: LLMs write code, you ship it. Reality: LLMs accelerate boilerplate, but architectural decisions, critical review, and judgment are still yours. Real value is speed in exploration and prototyping. You iterate 10x faster, if you know what you're iterating.",
      },
    },
    {
      slug: "relational-vs-nonrelational",
      title: {
        pt: "Relacional vs Não-Relacional: trade-offs verdadeiros",
        en: "Relational vs Non-Relational: true trade-offs",
      },
      date: "2026-04-12",
      read: "10 min",
      kicker: { pt: "Bancos de Dados", en: "Databases" },
      summary: {
        pt: "SQL: ACID, integridade, rigidez. NoSQL: flexibilidade, escala, risco. Não há bala de prata.",
        en: "SQL: ACID, integrity, rigidity. NoSQL: flexibility, scale, risk. No silver bullet.",
      },
      excerpt: {
        pt: "SQL oferece: ACID, integridade referencial, queries ad-hoc. Custa em: rigidez de schema, escalabilidade horizontal limitada. NoSQL oferece: flexibilidade, escalabilidade horizontal, low-latency. Custa em: consistency, manualidade de relações, queries podem virar O(n) rápido. Não há bala de prata — e muitos projetos ganham com híbrido.",
        en: "SQL offers: ACID, referential integrity, ad-hoc queries. Costs: schema rigidity, limited horizontal scalability. NoSQL offers: flexibility, horizontal scale, low-latency. Costs: consistency, manual relationships, queries can turn O(n) fast. No silver bullet — and many projects win with hybrid.",
      },
    },
    {
      slug: "why-typescript",
      title: {
        pt: "Por que usar TypeScript agora é não-negociável",
        en: "Why TypeScript is non-negotiable now",
      },
      date: "2026-04-11",
      read: "7 min",
      kicker: { pt: "Linguagens", en: "Languages" },
      summary: {
        pt: "Types forçam validação. Overhead 15-20% em escrita, 3x mais rápido em refatoração. Documentação executável.",
        en: "Types enforce validation. 15-20% overhead, 3x faster refactoring. Executable documentation.",
      },
      excerpt: {
        pt: "Sem types, você está confiando que seu time vai lembrar de validar inputs na hora certa. TypeScript te força. É overhead em escrita inicial (15-20%), ganho em confiança e velocidade de refatoração (3x mais rápido). Para teams: ts-node + tsx tornam a barreira zero. Para maintainability: types são documentação executável que não pode sair de sincronia.",
        en: "Without types, you're trusting your team to remember to validate inputs at exactly the right moment. TypeScript enforces it. It's overhead in initial writing (15-20%), gains in confidence and refactor speed (3x faster). For teams: ts-node + tsx make the barrier zero. For maintainability: types are executable documentation that can't drift.",
      },
    },
    {
      slug: "clean-code-2026",
      title: {
        pt: "Clean Code em 2026: pragmatismo acima de dogma",
        en: "Clean Code in 2026: pragmatism over dogma",
      },
      date: "2026-04-10",
      read: "14 min",
      kicker: { pt: "Arquitetura", en: "Architecture" },
      summary: {
        pt: "Clean Code 2008 vs 2026: IA lê código ruim pra você, então mude o foco. Agora: auditabilidade, rastreabilidade de decisões, observabilidade. Por quê > O quê. Clean é meio, não fim.",
        en: "Clean Code 2008 vs 2026: AI reads bad code for you, so shift focus. Now: auditability, decision traceability, observability. Why > What. Clean is means, not end.",
      },
      excerpt: {
        pt: "## O Problema: Clean Code Virou Dogma\n\nClean Code de 2008 (Uncle Bob) era necessário:\n• Nomes descritivos (vs `a`, `x`, `temp`)\n• Funções pequenas (vs 500-line methods)\n• Sem comentários (vs explicar código ruim)\n• Single Responsibility (vs fazer 10 coisas)\n\n**Verdade da época:** Humanos leem código 10x mais que escrevem. Legibilidade era o gargalo.\n\n**Verdade de 2026:** IA lê seu código e te avisa quando tá ruim. Copilot, ChatGPT, Cursor — você não tá só legível pra humanos, tá legível pra máquinas.\n\n## O Que Mudou\n\n### 1. Código Ruim Não Passa Despercebido\n\nAntes (2008):\n```php\n// Sem IA, isso passa (humano cansado não vê)\nfunction process($d) { // Param `$d` é vago\n  $r = [];\n  foreach ($d as $v) {\n    if ($v['s'] == 'ok') { // Magic string 's'\n      $r[] = $v['p']; // What is 'p'?\n    }\n  }\n  return $r;\n}\n```\n\nAgora (2026):\n```php\n// Cursor/Copilot avisa imediatamente\nfunction process($data) { // WARN: Ambiguous variable name\n  $result = [];\n  foreach ($data as $item) {\n    if ($item['status'] == 'ok') { // LINT: Use constant\n      $result[] = $item['price']; // SUGGEST: Extract to method\n    }\n  }\n  return $result;\n}\n```\n\n**Conclusão:** Você não precisa que seu código seja \"bonito pra humanos lerem\". Precisa que seja **explicável em auditoria**.\n\n### 2. Observabilidade Substitui Legibilidade\n\nAntes era:\n\"Se o código é limpo, você entende o que tá acontecendo\"\n\nAgora é:\n\"Se o código tá rodando em produção, você precisa ver:\n• Qual função rodou\n• Com quais inputs\n• Quanto tempo levou\n• Qual decisão tomou\n• Por que falhou\"\n\n```php\n// ❌ ERRADO: Legível mas sem trace\npublic function transfer($from, $to, $amount)\n{\n  if ($from->balance < $amount) {\n    return false; // Falhou, mas por quê?\n  }\n  \n  $from->balance -= $amount;\n  $to->balance += $amount;\n  \n  return true;\n}\n\n// ✅ CORRETO: Observável\npublic function transfer($from, $to, $amount)\n{\n  Log::info('Transfer initiated', [\n    'from_id' => $from->id,\n    'to_id' => $to->id,\n    'amount' => $amount,\n  ]);\n  \n  if ($from->balance < $amount) {\n    Log::warning('Transfer failed: insufficient balance', [\n      'from_id' => $from->id,\n      'required' => $amount,\n      'balance' => $from->balance,\n      'shortfall' => $amount - $from->balance,\n    ]);\n    return false; // Agora você SABEporquê falhou\n  }\n  \n  $from->balance -= $amount;\n  $to->balance += $amount;\n  $from->save();\n  $to->save();\n  \n  Log::info('Transfer completed', [\n    'from_balance' => $from->balance,\n    'to_balance' => $to->balance,\n  ]);\n  \n  return true;\n}\n```\n\n### 3. Rastreabilidade > Pureza Arquitetural\n\nAntes:\n\"Seu código precisa respeitar Clean Code principles\"\n\nAgora:\n\"Se você não consegue rastrear uma decisão até a pessoa/ticket que pediu, o código tá incompleto\"\n\n```php\n// ❌ ERRADO: Decision misterioso\nif ($user->created_at->diffInDays() > 30) {\n  $discount = 0.1; // Por que 10%? Quem pediu?\n}\n\n// ✅ CORRETO: Rastreável\n// TICKET-4521: Loyalty discount for 30+ day users\n// @requester: Product team\n// @date: 2026-03-15\nif ($user->created_at->diffInDays() > 30) {\n  $discount = 0.1; // Loyalty program tier\n  \n  Log::info('Loyalty discount applied', [\n    'user_id' => $user->id,\n    'discount_percent' => $discount * 100,\n    'ticket' => 'TICKET-4521',\n  ]);\n}\n```\n\n## Novo Foco: Auditabilidade\n\n### O Que Significa Auditável?\n\n**Sua função auditável é quando:**\n\n1. **Input is logged**\n```php\npublic function processOrder(Order $order)\n{\n  Log::info('Order processing start', [\n    'order_id' => $order->id,\n    'items_count' => $order->items->count(),\n    'total' => $order->total,\n  ]);\n  // ...\n}\n```\n\n2. **Decisions are logged with reason**\n```php\nif ($order->total > 1000) {\n  // DECISION: High-value order, require approval\n  $order->status = 'pending_approval';\n  Log::info('Order flagged for approval', ['reason' => 'high_value']);\n} else {\n  $order->status = 'processing';\n  Log::info('Order auto-approved', ['reason' => 'below_threshold']);\n}\n```\n\n3. **Failures are specific**\n```php\ntry {\n  $payment = Stripe::charge($order->total);\n} catch (StripeException $e) {\n  Log::error('Payment failed', [\n    'error_code' => $e->getCode(),\n    'message' => $e->getMessage(),\n    'order_id' => $order->id,\n    'timestamp' => now(),\n  ]);\n  // DON'T just return false\n  throw $e; // Rastreável no APM\n}\n```\n\n4. **Output is logged**\n```php\npublic function processOrder(Order $order)\n{\n  // ... process ...\n  \n  Log::info('Order processed', [\n    'order_id' => $order->id,\n    'new_status' => $order->status,\n    'items_processed' => $processed_count,\n    'duration_ms' => now()->diffInMilliseconds($start_time),\n  ]);\n  \n  return $order;\n}\n```\n\n## Checklist: Code é Auditável?\n\n**SIM a:**\n• Cada decisão tem log de por que foi tomada\n• Falhas têm erro code específico + contexto\n• Chamadas externas (API, DB) são rastreadas\n• Inputs/outputs relevantes estão logados\n• Tempo de execução é monitorado\n• Mudanças de estado têm trail\n\n**NÃO:**\n• Só porque é legível pra humano\n• Só porque segue Clean Code principles\n• Só porque o código é \"bonito\"\n• Se não tem observability\n\n## Exemplo: Auditável vs Bonito\n\n```php\n// ✅ AUDITÁVEL (não é o mais \"clean\")\npublic function updateUserStatus(User $user, string $newStatus)\n{\n  $oldStatus = $user->status;\n  \n  Log::info('User status update initiated', [\n    'user_id' => $user->id,\n    'from' => $oldStatus,\n    'to' => $newStatus,\n    'triggered_by' => auth()->id(),\n  ]);\n  \n  // Validação com logging\n  $validTransitions = [\n    'active' => ['suspended', 'inactive'],\n    'suspended' => ['active', 'inactive'],\n    'inactive' => ['active'],\n  ];\n  \n  if (!in_array($newStatus, $validTransitions[$oldStatus] ?? [])) {\n    Log::warning('Invalid status transition', [\n      'user_id' => $user->id,\n      'from' => $oldStatus,\n      'to' => $newStatus,\n      'allowed_transitions' => $validTransitions[$oldStatus] ?? [],\n    ]);\n    throw new InvalidStatusTransitionException();\n  }\n  \n  $user->status = $newStatus;\n  $user->save();\n  \n  Log::info('User status updated', [\n    'user_id' => $user->id,\n    'new_status' => $user->status,\n    'timestamp' => now(),\n  ]);\n  \n  event(new UserStatusChanged($user, $oldStatus));\n}\n```\n\nCompara com esse \"Clean Code puro\":\n\n```php\n// ❌ BONITO MAS NÃO AUDITÁVEL\npublic function updateUserStatus(User $user, string $newStatus)\n{\n  if (!$this->isValidTransition($user->status, $newStatus)) {\n    throw new InvalidStatusTransitionException();\n  }\n  \n  $user->status = $newStatus;\n  $user->save();\n  event(new UserStatusChanged($user));\n}\n\nprivate function isValidTransition(string $from, string $to): bool\n{\n  return in_array($to, $this->allowedTransitions[$from] ?? []);\n}\n```\n\nO segundo é mais \"clean\", mas:\n• Não sabe quem pediu mudança\n• Não loga decisão (why?)\n• Não diferencia erro (qual transição inválida?)\n• Impossível auditar em produção\n\n## Clean Code em 2026: O Novo Contrato\n\n**De:**\n• Nomes bonitos\n• Funções pequenas\n• Sem comentários\n\n**Para:**\n• Nomes rastreáveis (IDs, contexto)\n• Funções com logging de decisão\n• Documentação de decisão (ticket, requester, date)\n\n## Resumo\n\n**Clean Code 2008 = Legibilidade**\n**Clean Code 2026 = Auditabilidade + Observabilidade + Rastreabilidade**\n\nSeu código não precisa ser o mais bonito.\nPrecisa deixar pistas de por que fez cada coisa.",
        en: "## The Problem: Clean Code Became Dogma\n\nClean Code from 2008 (Uncle Bob) was necessary:\n• Descriptive names (vs `a`, `x`, `temp`)\n• Small functions (vs 500-line methods)\n• No comments (vs explaining bad code)\n• Single Responsibility (vs doing 10 things)\n\n**Truth of the time:** Humans read code 10x more than they write. Readability was the bottleneck.\n\n**Truth of 2026:** AI reads your code and warns you when it's bad. Copilot, ChatGPT, Cursor — you're not just readable for humans, you're readable for machines.\n\n## What Changed\n\n### 1. Bad Code Doesn't Escape Notice\n\nBefore (2008):\n```php\n// Without AI, this passes (tired human misses it)\nfunction process($d) { // Param `$d` is vague\n  $r = [];\n  foreach ($d as $v) {\n    if ($v['s'] == 'ok') { // Magic string 's'\n      $r[] = $v['p']; // What is 'p'?\n    }\n  }\n  return $r;\n}\n```\n\nNow (2026):\n```php\n// Cursor/Copilot warns immediately\nfunction process($data) { // WARN: Ambiguous variable name\n  $result = [];\n  foreach ($data as $item) {\n    if ($item['status'] == 'ok') { // LINT: Use constant\n      $result[] = $item['price']; // SUGGEST: Extract to method\n    }\n  }\n  return $result;\n}\n```\n\n**Conclusion:** You don't need your code to be \"pretty for humans to read\". You need it to be **explainable in audit**.\n\n### 2. Observability Replaces Readability\n\nBefore was:\n\"If code is clean, you understand what's happening\"\n\nNow is:\n\"If code is running in production, you need to see:\n• Which function ran\n• With which inputs\n• How long it took\n• What decision it made\n• Why it failed\"\n\n```php\n// ❌ WRONG: Readable but no trace\npublic function transfer($from, $to, $amount)\n{\n  if ($from->balance < $amount) {\n    return false; // Failed, but why?\n  }\n  \n  $from->balance -= $amount;\n  $to->balance += $amount;\n  \n  return true;\n}\n\n// ✅ CORRECT: Observable\npublic function transfer($from, $to, $amount)\n{\n  Log::info('Transfer initiated', [\n    'from_id' => $from->id,\n    'to_id' => $to->id,\n    'amount' => $amount,\n  ]);\n  \n  if ($from->balance < $amount) {\n    Log::warning('Transfer failed: insufficient balance', [\n      'from_id' => $from->id,\n      'required' => $amount,\n      'balance' => $from->balance,\n      'shortfall' => $amount - $from->balance,\n    ]);\n    return false; // Now you KNOW why it failed\n  }\n  \n  $from->balance -= $amount;\n  $to->balance += $amount;\n  $from->save();\n  $to->save();\n  \n  Log::info('Transfer completed', [\n    'from_balance' => $from->balance,\n    'to_balance' => $to->balance,\n  ]);\n  \n  return true;\n}\n```\n\n### 3. Traceability > Architectural Purity\n\nBefore:\n\"Your code needs to respect Clean Code principles\"\n\nNow:\n\"If you can't trace a decision back to the person/ticket that requested it, the code is incomplete\"\n\n```php\n// ❌ WRONG: Mysterious decision\nif ($user->created_at->diffInDays() > 30) {\n  $discount = 0.1; // Why 10%? Who asked?\n}\n\n// ✅ CORRECT: Traceable\n// TICKET-4521: Loyalty discount for 30+ day users\n// @requester: Product team\n// @date: 2026-03-15\nif ($user->created_at->diffInDays() > 30) {\n  $discount = 0.1; // Loyalty program tier\n  \n  Log::info('Loyalty discount applied', [\n    'user_id' => $user->id,\n    'discount_percent' => $discount * 100,\n    'ticket' => 'TICKET-4521',\n  ]);\n}\n```\n\n## New Focus: Auditability\n\n### What Does Auditable Mean?\n\n**Your function is auditable when:**\n\n1. **Input is logged**\n```php\npublic function processOrder(Order $order)\n{\n  Log::info('Order processing start', [\n    'order_id' => $order->id,\n    'items_count' => $order->items->count(),\n    'total' => $order->total,\n  ]);\n  // ...\n}\n```\n\n2. **Decisions are logged with reason**\n```php\nif ($order->total > 1000) {\n  // DECISION: High-value order, require approval\n  $order->status = 'pending_approval';\n  Log::info('Order flagged for approval', ['reason' => 'high_value']);\n} else {\n  $order->status = 'processing';\n  Log::info('Order auto-approved', ['reason' => 'below_threshold']);\n}\n```\n\n3. **Failures are specific**\n```php\ntry {\n  $payment = Stripe::charge($order->total);\n} catch (StripeException $e) {\n  Log::error('Payment failed', [\n    'error_code' => $e->getCode(),\n    'message' => $e->getMessage(),\n    'order_id' => $order->id,\n    'timestamp' => now(),\n  ]);\n  // DON'T just return false\n  throw $e; // Traceable in APM\n}\n```\n\n4. **Output is logged**\n```php\npublic function processOrder(Order $order)\n{\n  // ... process ...\n  \n  Log::info('Order processed', [\n    'order_id' => $order->id,\n    'new_status' => $order->status,\n    'items_processed' => $processed_count,\n    'duration_ms' => now()->diffInMilliseconds($start_time),\n  ]);\n  \n  return $order;\n}\n```\n\n## Checklist: Is Code Auditable?\n\n**YES to:**\n• Each decision has log of why it was made\n• Failures have specific error code + context\n• External calls (API, DB) are traced\n• Relevant inputs/outputs are logged\n• Execution time is monitored\n• State changes have trail\n\n**NO to:**\n• Just because it's readable to humans\n• Just because it follows Clean Code principles\n• Just because code is \"pretty\"\n• If it lacks observability\n\n## Example: Auditable vs Pretty\n\n```php\n// ✅ AUDITABLE (not the most \"clean\")\npublic function updateUserStatus(User $user, string $newStatus)\n{\n  $oldStatus = $user->status;\n  \n  Log::info('User status update initiated', [\n    'user_id' => $user->id,\n    'from' => $oldStatus,\n    'to' => $newStatus,\n    'triggered_by' => auth()->id(),\n  ]);\n  \n  // Validation with logging\n  $validTransitions = [\n    'active' => ['suspended', 'inactive'],\n    'suspended' => ['active', 'inactive'],\n    'inactive' => ['active'],\n  ];\n  \n  if (!in_array($newStatus, $validTransitions[$oldStatus] ?? [])) {\n    Log::warning('Invalid status transition', [\n      'user_id' => $user->id,\n      'from' => $oldStatus,\n      'to' => $newStatus,\n      'allowed_transitions' => $validTransitions[$oldStatus] ?? [],\n    ]);\n    throw new InvalidStatusTransitionException();\n  }\n  \n  $user->status = $newStatus;\n  $user->save();\n  \n  Log::info('User status updated', [\n    'user_id' => $user->id,\n    'new_status' => $user->status,\n    'timestamp' => now(),\n  ]);\n  \n  event(new UserStatusChanged($user, $oldStatus));\n}\n```\n\nCompare with this \"pure Clean Code\":\n\n```php\n// ❌ PRETTY BUT NOT AUDITABLE\npublic function updateUserStatus(User $user, string $newStatus)\n{\n  if (!$this->isValidTransition($user->status, $newStatus)) {\n    throw new InvalidStatusTransitionException();\n  }\n  \n  $user->status = $newStatus;\n  $user->save();\n  event(new UserStatusChanged($user));\n}\n\nprivate function isValidTransition(string $from, string $to): bool\n{\n  return in_array($to, $this->allowedTransitions[$from] ?? []);\n}\n```\n\nThe second is more \"clean\", but:\n• Doesn't know who requested the change\n• Doesn't log decision (why?)\n• Doesn't differentiate error (which transition invalid?)\n• Impossible to audit in production\n\n## Clean Code in 2026: The New Contract\n\n**From:**\n• Pretty names\n• Small functions\n• No comments\n\n**To:**\n• Traceable names (IDs, context)\n• Functions with decision logging\n• Decision documentation (ticket, requester, date)\n\n## Summary\n\n**Clean Code 2008 = Readability**\n**Clean Code 2026 = Auditability + Observability + Traceability**\n\nYour code doesn't need to be the prettiest.\nIt needs to leave clues of why it did each thing.",
      },
    },
    {
      slug: "service-layers-laravel",
      title: {
        pt: "Service Layers em Laravel 12: onde vai a lógica mesmo?",
        en: "Service Layers in Laravel 12: where does logic actually go?",
      },
      date: "2026-04-07",
      read: "18 min",
      kicker: { pt: "Arquitetura", en: "Architecture" },
      summary: {
        pt: "Controller → Service → Repository → Model. Quem faz o quê? Controller desserializa HTTP. Service orquestra negócio. Repository faz queries. Model são dados. A regra: se toca database + lógica, tira do Controller.",
        en: "Controller → Service → Repository → Model. Who does what? Controller deserializes HTTP. Service orchestrates business. Repository queries. Model is data. The rule: if it touches database + logic, remove it from Controller.",
      },
      excerpt: {
        pt: "## O Problema: HTTP Logic Poluído\n\nControllers que fazem queries, Controllers que manipulam dados, Controllers que orquestram transações — tudo espalhado.\n\n## A Estrutura Correta\n\n**Controller** — apenas adaptador HTTP\n• Desserializa request\n• Valida input\n• Chama Service\n• Serializa response\n\n**Service** — orquestração de negócio\n• Validações de domínio\n• Regras de negócio\n• Transações DB\n• Coordena Repositories\n\n**Repository** — acesso puro a dados\n• Queries sem lógica\n• Mutations sem lógica\n• Zero regras de negócio\n\n**Model** — representação de dados\n• Relacionamentos\n• Casts e accessors\n• Validações básicas\n\n## Exemplo: Transferência Bancária\n\n### ❌ ERRADO (tudo no Controller)\n\n```php\nclass TransferController {\n  public function transfer(Request $request) {\n    $from = Account::find($request->from_id);\n    $to = Account::find($request->to_id);\n    if ($from->balance < $request->amount) abort(400);\n    $from->balance -= $request->amount;\n    $to->balance += $request->amount;\n    $from->save();\n    $to->save();\n    AuditLog::create([...]);\n  }\n}\n```\n\n**Problemas:**\n- Impossível testar isolado\n- Lógica espalhada\n- Reutilização zero\n- Difícil de mudar\n- Sem transação de verdade\n\n### ✅ CORRETO (Controller delega)\n\n```php\nclass TransferController {\n  public function transfer(Request $request, TransferService $service) {\n    $validated = $request->validate([...]);\n    $result = $service->transfer($validated);\n    return response()->json($result);\n  }\n}\n\nclass TransferService {\n  public function transfer(array $data): array {\n    return DB::transaction(function () use ($data) {\n      $from = $this->accountRepo->findOrFail($data['from_id']);\n      $to = $this->accountRepo->findOrFail($data['to_id']);\n      \n      // Validações de negócio\n      if (!$from->canTransfer($data['amount'])) {\n        throw new InsufficientFundsException();\n      }\n      \n      // Ledger duplo\n      $this->ledger->debit($from, $data['amount']);\n      $this->ledger->credit($to, $data['amount']);\n      \n      // Auditoria\n      $this->auditLog->record('transfer', $data);\n      \n      return ['success' => true, 'txn_id' => ...];\n    });\n  }\n}\n\nclass AccountRepository {\n  public function findOrFail(int $id): Account {\n    return Account::findOrFail($id);\n  }\n}\n```\n\n**Vantagens:**\n- Testável (testa Service isolado)\n- Transação real com rollback automático\n- Auditória garantida\n- Reutilizável (HTTP, CLI, Queue)\n- Fácil de mudar\n\n## Laravel 12: Actions vs Services\n\n**Quando usar Actions (Spatie)**\n→ Operações simples e unitárias\n→ Um único fluxo de dados\n→ Exemplos: CreatePost, SendEmail, UpdateProfile\n\n**Quando usar Services**\n→ Fluxos complexos com múltiplos passos\n→ Múltiplas entidades envolvidas\n→ Exemplos: Onboarding, Pagamento, Migração\n\n## Benefícios Práticos\n\n**Testabilidade**\n• Testa Service sem Controller\n• Testa Controller sem Service (mock)\n• Cobertura de testes real\n\n**Reusabilidade**\n• Mesma lógica por HTTP, CLI, Queue\n• Mesma lógica por diferentes controllers\n• Sem duplicação\n\n**Manutenibilidade**\n• Lógica em um único lugar\n• Mudanças seguras e isoladas\n• Debugging direto ao ponto\n\n**Escalabilidade**\n• Estrutura preparada para crescimento\n• Fácil extrair para microsserviço\n• Separação clara de responsabilidades\n\n## A Regra de Ouro\n\n**Se toca em database + lógica = tira do Controller.**\n\nNão é sobre microserviços ou complexidade — é sobre separação clara. A estrutura de camadas funciona em qualquer escala.",
        en: "## The Problem: HTTP Logic Pollution\n\nControllers doing queries, Controllers manipulating data, Controllers orchestrating transactions — scattered everywhere.\n\n## The Correct Structure\n\n**Controller** — just HTTP adapter\n• Deserializes request\n• Validates input\n• Calls Service\n• Serializes response\n\n**Service** — business orchestration\n• Domain validations\n• Business rules\n• DB transactions\n• Coordinates Repositories\n\n**Repository** — pure data access\n• Queries without logic\n• Mutations without logic\n• Zero business rules\n\n**Model** — data representation\n• Relationships\n• Casts and accessors\n• Basic validations\n\n## Example: Bank Transfer\n\n### ❌ WRONG (all in Controller)\n\n```php\nclass TransferController {\n  public function transfer(Request $request) {\n    $from = Account::find($request->from_id);\n    $to = Account::find($request->to_id);\n    if ($from->balance < $request->amount) abort(400);\n    $from->balance -= $request->amount;\n    $to->balance += $request->amount;\n    $from->save();\n    $to->save();\n    AuditLog::create([...]);\n  }\n}\n```\n\n**Problems:**\n- Impossible to test isolated\n- Logic scattered\n- Zero reusability\n- Hard to change\n- No real transaction\n\n### ✅ CORRECT (Controller delegates)\n\n```php\nclass TransferController {\n  public function transfer(Request $request, TransferService $service) {\n    $validated = $request->validate([...]);\n    $result = $service->transfer($validated);\n    return response()->json($result);\n  }\n}\n\nclass TransferService {\n  public function transfer(array $data): array {\n    return DB::transaction(function () use ($data) {\n      $from = $this->accountRepo->findOrFail($data['from_id']);\n      $to = $this->accountRepo->findOrFail($data['to_id']);\n      \n      // Business validations\n      if (!$from->canTransfer($data['amount'])) {\n        throw new InsufficientFundsException();\n      }\n      \n      // Double-entry ledger\n      $this->ledger->debit($from, $data['amount']);\n      $this->ledger->credit($to, $data['amount']);\n      \n      // Audit trail\n      $this->auditLog->record('transfer', $data);\n      \n      return ['success' => true, 'txn_id' => ...];\n    });\n  }\n}\n\nclass AccountRepository {\n  public function findOrFail(int $id): Account {\n    return Account::findOrFail($id);\n  }\n}\n```\n\n**Advantages:**\n- Testable (test Service isolated)\n- Real transaction with auto-rollback\n- Guaranteed audit trail\n- Reusable (HTTP, CLI, Queue)\n- Easy to change\n\n## Laravel 12: Actions vs Services\n\n**When to use Actions (Spatie)**\n→ Simple unitary operations\n→ Single data flow\n→ Examples: CreatePost, SendEmail, UpdateProfile\n\n**When to use Services**\n→ Complex flows with multiple steps\n→ Multiple entities involved\n→ Examples: Onboarding, Payment, Migration\n\n## Practical Benefits\n\n**Testability**\n• Test Service without Controller\n• Test Controller without Service (mock)\n• Real test coverage\n\n**Reusability**\n• Same logic via HTTP, CLI, Queue\n• Same logic via different controllers\n• No duplication\n\n**Maintainability**\n• Logic in single place\n• Safe isolated changes\n• Straight debugging\n\n**Scalability**\n• Structure ready for growth\n• Easy to extract to microservice\n• Clear separation of concerns\n\n## The Golden Rule\n\n**If it touches database + logic = remove it from Controller.**\n\nNot about microservices or complexity — it's about clean separation. Layered structure works at any scale.",
      },
    },
    {
      slug: "scaling-laravel-no-rewrite",
      title: {
        pt: "Escalando Laravel sem reescrever: caching, queues, read models",
        en: "Scaling Laravel without rewrite: caching, queues, read models",
      },
      date: "2026-04-18",
      read: "16 min",
      kicker: { pt: "Infra", en: "Infrastructure" },
      summary: {
        pt: "Seu app ficou lento? Antes de reescrever em Go, tenta isso: cache em camadas, queues para jobs pesados, read models para queries complexas, índices no banco. 9 de 10 vezes você só precisa remover queries N+1 e cachear a homepage.",
        en: "App got slow? Before rewriting in Go, try this: layered caching, queues for heavy jobs, read models for complex queries, database indexes. 9 out of 10 times you just need to remove N+1 queries and cache the homepage.",
      },
      excerpt: {
        pt: "## O Mito da Reescrita\n\nSeu Laravel tá lento. Você quer reescrever em Go, Node, Rust.\n\n**Pare.**\n\n90% dos cases que vi, o problema não é Laravel — é:\n• Queries N+1\n• Sem cache de homepage\n• Processing síncrono de jobs pesados\n• Índices faltando no banco\n• Serialização ineficiente\n\n## Diagnóstico: Onde Você Tá Lento?\n\n### 1. Check N+1 Queries\n\n```php\n// ❌ ERRADO\n$users = User::all(); // 1 query\nforeach ($users as $user) {\n  echo $user->profile->bio; // 1 query por usuário\n}\n// Total: 1 + 101 queries para 100 usuários\n\n// ✅ CORRETO\n$users = User::with('profile')->get(); // 2 queries\nforeach ($users as $user) {\n  echo $user->profile->bio;\n}\n```\n\n**Como encontrar N+1:**\n• `php artisan debugbar` - QueryLogger mostra duplicatas\n• `DB::listen()` - log todas as queries em produção\n• Procure por queries idênticas em loops\n\n```php\nDB::listen(function ($query) {\n  if ($query->time > 50) { // Queries lentas\n    Log::warning('Slow query', [\n      'sql' => $query->sql,\n      'bindings' => $query->bindings,\n      'time' => $query->time,\n    ]);\n  }\n});\n```\n\n### 2. Profile com Blackfire\n\n```bash\nbrew install blackfire-agent\nblackfire-cli run curl https://seu-app.com\n```\n\n**Você verá:**\n• Função mais lenta\n• Chamadas repetidas\n• Tempo em DB vs PHP\n• Memory leaks\n\n## Estratégia 1: Caching em Camadas\n\n### Cache Level 1: HTTP Response (Browser)\n\n```php\n// resources/views/products/index.blade.php\n<meta http-equiv=\"cache-control\" content=\"max-age=3600\">\n\n// Laravel (middleware)\nRoute::middleware('cache:3600')->group(function () {\n  Route::get('/products', [ProductController::class, 'index']);\n});\n```\n\n### Cache Level 2: Application Cache (Redis)\n\n```php\n// app/Http/Controllers/ProductController.php\npublic function index()\n{\n  return Cache::remember('products.all', 3600, function () {\n    return Product::with('category')\n      ->select('id', 'name', 'price', 'category_id')\n      ->get();\n  });\n}\n\n// Quando atualizar um produto\npublic function update(Request $request, Product $product)\n{\n  $product->update($request->validated());\n  Cache::forget('products.all'); // Invalida cache\n  return redirect()->back();\n}\n```\n\n### Cache Level 3: Query Result Cache\n\n```php\n// app/Repositories/ProductRepository.php\npublic function getActivePriced()\n{\n  $key = 'products:active:priced';\n  $ttl = now()->addHours(1);\n  \n  return Cache::remember($key, $ttl, function () {\n    return Product::where('active', true)\n      ->whereNotNull('price')\n      ->select('id', 'name', 'price')\n      ->orderBy('name')\n      ->get();\n  });\n}\n```\n\n### Cache Level 4: Database Query Cache (Índices)\n\n```php\n// database/migrations/2026_04_18_add_indexes.php\nSchema::table('products', function (Blueprint $table) {\n  $table->index('active');\n  $table->index(['category_id', 'active']);\n  $table->index('created_at');\n  $table->fullText(['name', 'description']);\n});\n\n// Query agora é muito mais rápida\nProduct::whereActive(true)\n  ->whereCategory(5)\n  ->orderByDesc('created_at')\n  ->get();\n```\n\n## Estratégia 2: Async Processing com Queues\n\n### Problema: Requisição Bloqueada\n\n```php\n// ❌ ERRADO - Usuário espera\npublic function checkout(Request $request)\n{\n  $order = Order::create($request->validated());\n  \n  // Email leva 2 segundos\n  Mail::send(new OrderConfirmation($order));\n  \n  // PDF leva 3 segundos\n  $pdf = PDF::generate($order);\n  $order->update(['pdf_path' => $pdf]);\n  \n  // SMS leva 1 segundo\n  Twilio::send($order->customer->phone);\n  \n  return response()->json(['order' => $order]); // 6 segundos depois\n}\n```\n\n### Solução: Dispatch para Queues\n\n```php\n// ✅ CORRETO - Usuário vê resposta em 50ms\npublic function checkout(Request $request)\n{\n  $order = Order::create($request->validated());\n  \n  // Dispatch para background jobs\n  SendOrderConfirmation::dispatch($order);\n  GenerateOrderPDF::dispatch($order);\n  SendOrderSMS::dispatch($order);\n  \n  return response()->json([\n    'order' => $order,\n    'status' => 'processing',\n  ]); // Resposta imediata\n}\n\n// app/Jobs/SendOrderConfirmation.php\nclass SendOrderConfirmation implements ShouldQueue\n{\n  use Dispatchable, InteractsWithQueue, Queueable, SerializesModels;\n  \n  public function __construct(public Order $order) {}\n  \n  public function handle()\n  {\n    Mail::send(new OrderConfirmation($this->order));\n  }\n}\n\n// app/Jobs/GenerateOrderPDF.php\nclass GenerateOrderPDF implements ShouldQueue\n{\n  use Dispatchable, InteractsWithQueue, Queueable, SerializesModels;\n  \n  public function __construct(public Order $order) {}\n  \n  public function handle()\n  {\n    $pdf = PDF::generate($this->order);\n    $this->order->update(['pdf_path' => $pdf]);\n    \n    // Dispara outro job quando terminar\n    NotifySlackOrderReady::dispatch($this->order);\n  }\n}\n```\n\n### Configurar Redis + Supervisor\n\n```bash\n# .env\nQUEUE_CONNECTION=redis\nREDIS_HOST=127.0.0.1\nREDIS_PORT=6379\n\n# supervisord.conf\n[program:laravel-worker]\nprocess_name=%(program_name)s_%(process_num)02d\ncommand=php /path/to/artisan queue:work redis --sleep=3 --tries=3\nautostart=true\nnumprocs=4\nredirect_stderr=true\n```\n\n## Estratégia 3: Read Models (CQRS Light)\n\n### Problema: Queries Complexas\n\n```php\n// ❌ ERRADO - Calcula tudo em tempo real\npublic function dashboard()\n{\n  $totalSales = Order::whereMonth('created_at', now()->month)\n    ->sum('total');\n  \n  $topProducts = Order::with('items')\n    ->whereMonth('created_at', now()->month)\n    ->get()\n    ->flatMap->items\n    ->groupBy('product_id')\n    ->map(fn ($items) => [\n      'product_id' => $items->first()->product_id,\n      'quantity' => $items->count(),\n      'revenue' => $items->sum('price'),\n    ])\n    ->sortByDesc('revenue')\n    ->take(10);\n  \n  $customerMetrics = Customer::with('orders')\n    ->get()\n    ->map(fn ($customer) => [\n      'id' => $customer->id,\n      'name' => $customer->name,\n      'lifetime_value' => $customer->orders->sum('total'),\n      'order_count' => $customer->orders->count(),\n    ]);\n  \n  return view('dashboard', [\n    'totalSales' => $totalSales,\n    'topProducts' => $topProducts,\n    'customerMetrics' => $customerMetrics,\n  ]);\n}\n```\n\n### Solução: Pré-calcular em Read Model\n\n```php\n// database/migrations/2026_04_18_create_dashboard_metrics.php\nSchema::create('dashboard_metrics', function (Blueprint $table) {\n  $table->id();\n  $table->integer('year');\n  $table->integer('month');\n  $table->decimal('total_sales', 12, 2);\n  $table->integer('order_count');\n  $table->decimal('avg_order_value', 12, 2);\n  $table->timestamps();\n  $table->unique(['year', 'month']);\n});\n\nSchema::create('top_products_read_model', function (Blueprint $table) {\n  $table->id();\n  $table->foreignIdFor(Product::class);\n  $table->integer('year');\n  $table->integer('month');\n  $table->integer('quantity_sold');\n  $table->decimal('total_revenue', 12, 2);\n  $table->timestamps();\n  $table->unique(['product_id', 'year', 'month']);\n});\n\n// app/Jobs/RecalculateDashboardMetrics.php\nclass RecalculateDashboardMetrics implements ShouldQueue\n{\n  public function handle()\n  {\n    $month = now();\n    \n    // Calcula agregação pesada\n    $metrics = Order::whereYear('created_at', $month->year)\n      ->whereMonth('created_at', $month->month)\n      ->aggregate([\n        'total_sales' => DB::raw('SUM(total)'),\n        'order_count' => DB::raw('COUNT(*)')\n      ]);\n    \n    DashboardMetric::updateOrCreate(\n      ['year' => $month->year, 'month' => $month->month],\n      [\n        'total_sales' => $metrics->total_sales,\n        'order_count' => $metrics->order_count,\n        'avg_order_value' => $metrics->total_sales / $metrics->order_count,\n      ]\n    );\n  }\n}\n\n// Controller agora é trivial\npublic function dashboard()\n{\n  $metrics = DashboardMetric::whereYear('year', now()->year)\n    ->whereMonth('month', now()->month)\n    ->first();\n  \n  $topProducts = TopProductsReadModel::where('year', now()->year)\n    ->where('month', now()->month)\n    ->orderByDesc('total_revenue')\n    ->limit(10)\n    ->get();\n  \n  return view('dashboard', [\n    'metrics' => $metrics,\n    'topProducts' => $topProducts,\n  ]);\n}\n```\n\n## Estratégia 4: Índices Inteligentes\n\n```php\n// database/migrations/2026_04_18_optimize_indexes.php\n\nSchema::table('orders', function (Blueprint $table) {\n  // Queries por status + data\n  $table->index(['status', 'created_at']);\n  \n  // Queries por customer\n  $table->index('customer_id');\n  \n  // Sorting por updated_at\n  $table->index('updated_at');\n  \n  // Full-text search\n  $table->fullText(['reference_number', 'notes']);\n});\n\nSchema::table('customers', function (Blueprint $table) {\n  // Filtro por email (login, busca)\n  $table->unique('email');\n  \n  // Sorting\n  $table->index('created_at');\n});\n\nSchema::table('order_items', function (Blueprint $table) {\n  // JOINs rápidos\n  $table->index('order_id');\n  $table->index('product_id');\n});\n```\n\n**EXPLAIN ANALYZE**\n\n```php\n// Antes do índice: Full table scan (lento)\nDB::statement('EXPLAIN ANALYZE SELECT * FROM orders WHERE status = \"pending\" AND created_at > NOW() - INTERVAL 7 DAY');\n\n// Depois do índice: Index range scan (rápido)\n```\n\n## Estratégia 5: Serialização Eficiente\n\n```php\n// ❌ ERRADO - Carrega tudo\nRoute::get('/api/products', function () {\n  return Product::with('category', 'reviews', 'inventory')\n    ->get();\n  // Response: 2MB para 100 produtos\n});\n\n// ✅ CORRETO - Seleciona só o necessário\nRoute::get('/api/products', function () {\n  return Product::select('id', 'name', 'price', 'category_id')\n    ->with([\n      'category:id,name',\n      'reviews:id,product_id,rating',\n    ])\n    ->get();\n  // Response: 150KB para 100 produtos\n});\n```\n\n## Checklist de Performance\n\n**Imediato (antes de segunda-feira):**\n• Remover N+1 queries com `with()`\n• Usar `select()` para trazer só colunas necessárias\n• Adicionar índices nas colunas de `where` e `orderBy`\n• Cache da homepage com `Cache::remember()`\n\n**Curto prazo (semana que vem):**\n• Mover jobs síncronos para queues\n• Implementar read models para dashboards\n• Usar Redis para cache de sessão\n• Monitor com Blackfire/Debugbar\n\n**Médio prazo (próximo mês):**\n• Database replication (read replicas)\n• CDN para assets\n• Elasticsearch para buscas complexas\n• Horizontal scaling com load balancer\n\n## Quando Realmente Precisa Reescrever\n\n**Reescrever SÓ se:**\n• Laravel chega em 100K RPS e precisa de 200K\n• Latência sub-milissegundo (trading, gaming)\n• Problema é real-time (websockets 24/7)\n• Team prefere outra linguagem (não é decisão de perf)\n\n**Não é caso para reescrever:**\n• App lento mas sem medir\n• \"Go é mais rápido que PHP\"\n• Reimplementar do zero \"certo\"\n• Porque novo tech é legal\n\n## Resumo\n\n**90% do tempo, escalar Laravel é:**\n1. Encontrar gargalo com profiler\n2. Cache em 3 níveis (browser, app, db)\n3. Mover work sync → async com queues\n4. Read models para agregações\n5. Índices inteligentes no banco\n\n**Tempo**: 1-2 sprints\n**Custo**: 0 (usa infra que já tem)\n**Risco**: Baixo (mudanças incrementais)\n\nReescrever leva 6 meses, custa 3x mais, e você descobre que Node também tá lento sem cache.",
        en: "## The Rewrite Myth\n\nYour Laravel is slow. You want to rewrite in Go, Node, Rust.\n\n**Stop.**\n\n90% of cases I've seen, the problem isn't Laravel — it's:\n• N+1 queries\n• No homepage caching\n• Synchronous heavy processing\n• Missing database indexes\n• Inefficient serialization\n\n## Diagnosis: Where Are You Slow?\n\n### 1. Check N+1 Queries\n\n```php\n// ❌ WRONG\n$users = User::all(); // 1 query\nforeach ($users as $user) {\n  echo $user->profile->bio; // 1 query per user\n}\n// Total: 1 + 101 queries for 100 users\n\n// ✅ CORRECT\n$users = User::with('profile')->get(); // 2 queries\nforeach ($users as $user) {\n  echo $user->profile->bio;\n}\n```\n\n**How to find N+1:**\n• `php artisan debugbar` - QueryLogger shows duplicates\n• `DB::listen()` - log all queries in production\n• Look for identical queries in loops\n\n```php\nDB::listen(function ($query) {\n  if ($query->time > 50) { // Slow queries\n    Log::warning('Slow query', [\n      'sql' => $query->sql,\n      'bindings' => $query->bindings,\n      'time' => $query->time,\n    ]);\n  }\n});\n```\n\n### 2. Profile with Blackfire\n\n```bash\nbrew install blackfire-agent\nblackfire-cli run curl https://your-app.com\n```\n\n**You'll see:**\n• Slowest function\n• Repeated calls\n• Time in DB vs PHP\n• Memory leaks\n\n## Strategy 1: Layered Caching\n\n### Cache Level 1: HTTP Response (Browser)\n\n```php\n// resources/views/products/index.blade.php\n<meta http-equiv=\"cache-control\" content=\"max-age=3600\">\n\n// Laravel (middleware)\nRoute::middleware('cache:3600')->group(function () {\n  Route::get('/products', [ProductController::class, 'index']);\n});\n```\n\n### Cache Level 2: Application Cache (Redis)\n\n```php\n// app/Http/Controllers/ProductController.php\npublic function index()\n{\n  return Cache::remember('products.all', 3600, function () {\n    return Product::with('category')\n      ->select('id', 'name', 'price', 'category_id')\n      ->get();\n  });\n}\n\n// When updating a product\npublic function update(Request $request, Product $product)\n{\n  $product->update($request->validated());\n  Cache::forget('products.all'); // Invalidates cache\n  return redirect()->back();\n}\n```\n\n### Cache Level 3: Query Result Cache\n\n```php\n// app/Repositories/ProductRepository.php\npublic function getActivePriced()\n{\n  $key = 'products:active:priced';\n  $ttl = now()->addHours(1);\n  \n  return Cache::remember($key, $ttl, function () {\n    return Product::where('active', true)\n      ->whereNotNull('price')\n      ->select('id', 'name', 'price')\n      ->orderBy('name')\n      ->get();\n  });\n}\n```\n\n### Cache Level 4: Database Query Cache (Indexes)\n\n```php\n// database/migrations/2026_04_18_add_indexes.php\nSchema::table('products', function (Blueprint $table) {\n  $table->index('active');\n  $table->index(['category_id', 'active']);\n  $table->index('created_at');\n  $table->fullText(['name', 'description']);\n});\n\n// Query is now much faster\nProduct::whereActive(true)\n  ->whereCategory(5)\n  ->orderByDesc('created_at')\n  ->get();\n```\n\n## Strategy 2: Async Processing with Queues\n\n### Problem: Blocked Request\n\n```php\n// ❌ WRONG - User waits\npublic function checkout(Request $request)\n{\n  $order = Order::create($request->validated());\n  \n  // Email takes 2 seconds\n  Mail::send(new OrderConfirmation($order));\n  \n  // PDF takes 3 seconds\n  $pdf = PDF::generate($order);\n  $order->update(['pdf_path' => $pdf]);\n  \n  // SMS takes 1 second\n  Twilio::send($order->customer->phone);\n  \n  return response()->json(['order' => $order]); // 6 seconds later\n}\n```\n\n### Solution: Dispatch to Queues\n\n```php\n// ✅ CORRECT - User sees response in 50ms\npublic function checkout(Request $request)\n{\n  $order = Order::create($request->validated());\n  \n  // Dispatch to background jobs\n  SendOrderConfirmation::dispatch($order);\n  GenerateOrderPDF::dispatch($order);\n  SendOrderSMS::dispatch($order);\n  \n  return response()->json([\n    'order' => $order,\n    'status' => 'processing',\n  ]); // Immediate response\n}\n\n// app/Jobs/SendOrderConfirmation.php\nclass SendOrderConfirmation implements ShouldQueue\n{\n  use Dispatchable, InteractsWithQueue, Queueable, SerializesModels;\n  \n  public function __construct(public Order $order) {}\n  \n  public function handle()\n  {\n    Mail::send(new OrderConfirmation($this->order));\n  }\n}\n\n// app/Jobs/GenerateOrderPDF.php\nclass GenerateOrderPDF implements ShouldQueue\n{\n  use Dispatchable, InteractsWithQueue, Queueable, SerializesModels;\n  \n  public function __construct(public Order $order) {}\n  \n  public function handle()\n  {\n    $pdf = PDF::generate($this->order);\n    $this->order->update(['pdf_path' => $pdf]);\n    \n    // Dispatch another job when done\n    NotifySlackOrderReady::dispatch($this->order);\n  }\n}\n```\n\n### Configure Redis + Supervisor\n\n```bash\n# .env\nQUEUE_CONNECTION=redis\nREDIS_HOST=127.0.0.1\nREDIS_PORT=6379\n\n# supervisord.conf\n[program:laravel-worker]\nprocess_name=%(program_name)s_%(process_num)02d\ncommand=php /path/to/artisan queue:work redis --sleep=3 --tries=3\nautostart=true\nnumprocs=4\nredirect_stderr=true\n```\n\n## Strategy 3: Read Models (CQRS Light)\n\n### Problem: Complex Queries\n\n```php\n// ❌ WRONG - Calculates everything in real-time\npublic function dashboard()\n{\n  $totalSales = Order::whereMonth('created_at', now()->month)\n    ->sum('total');\n  \n  $topProducts = Order::with('items')\n    ->whereMonth('created_at', now()->month)\n    ->get()\n    ->flatMap->items\n    ->groupBy('product_id')\n    ->map(fn ($items) => [\n      'product_id' => $items->first()->product_id,\n      'quantity' => $items->count(),\n      'revenue' => $items->sum('price'),\n    ])\n    ->sortByDesc('revenue')\n    ->take(10);\n  \n  $customerMetrics = Customer::with('orders')\n    ->get()\n    ->map(fn ($customer) => [\n      'id' => $customer->id,\n      'name' => $customer->name,\n      'lifetime_value' => $customer->orders->sum('total'),\n      'order_count' => $customer->orders->count(),\n    ]);\n  \n  return view('dashboard', [\n    'totalSales' => $totalSales,\n    'topProducts' => $topProducts,\n    'customerMetrics' => $customerMetrics,\n  ]);\n}\n```\n\n### Solution: Pre-calculate in Read Model\n\n```php\n// database/migrations/2026_04_18_create_dashboard_metrics.php\nSchema::create('dashboard_metrics', function (Blueprint $table) {\n  $table->id();\n  $table->integer('year');\n  $table->integer('month');\n  $table->decimal('total_sales', 12, 2);\n  $table->integer('order_count');\n  $table->decimal('avg_order_value', 12, 2);\n  $table->timestamps();\n  $table->unique(['year', 'month']);\n});\n\nSchema::create('top_products_read_model', function (Blueprint $table) {\n  $table->id();\n  $table->foreignIdFor(Product::class);\n  $table->integer('year');\n  $table->integer('month');\n  $table->integer('quantity_sold');\n  $table->decimal('total_revenue', 12, 2);\n  $table->timestamps();\n  $table->unique(['product_id', 'year', 'month']);\n});\n\n// app/Jobs/RecalculateDashboardMetrics.php\nclass RecalculateDashboardMetrics implements ShouldQueue\n{\n  public function handle()\n  {\n    $month = now();\n    \n    // Calculates heavy aggregation\n    $metrics = Order::whereYear('created_at', $month->year)\n      ->whereMonth('created_at', $month->month)\n      ->aggregate([\n        'total_sales' => DB::raw('SUM(total)'),\n        'order_count' => DB::raw('COUNT(*)')\n      ]);\n    \n    DashboardMetric::updateOrCreate(\n      ['year' => $month->year, 'month' => $month->month],\n      [\n        'total_sales' => $metrics->total_sales,\n        'order_count' => $metrics->order_count,\n        'avg_order_value' => $metrics->total_sales / $metrics->order_count,\n      ]\n    );\n  }\n}\n\n// Controller is now trivial\npublic function dashboard()\n{\n  $metrics = DashboardMetric::whereYear('year', now()->year)\n    ->whereMonth('month', now()->month)\n    ->first();\n  \n  $topProducts = TopProductsReadModel::where('year', now()->year)\n    ->where('month', now()->month)\n    ->orderByDesc('total_revenue')\n    ->limit(10)\n    ->get();\n  \n  return view('dashboard', [\n    'metrics' => $metrics,\n    'topProducts' => $topProducts,\n  ]);\n}\n```\n\n## Strategy 4: Smart Indexes\n\n```php\n// database/migrations/2026_04_18_optimize_indexes.php\n\nSchema::table('orders', function (Blueprint $table) {\n  // Queries by status + date\n  $table->index(['status', 'created_at']);\n  \n  // Queries by customer\n  $table->index('customer_id');\n  \n  // Sorting by updated_at\n  $table->index('updated_at');\n  \n  // Full-text search\n  $table->fullText(['reference_number', 'notes']);\n});\n\nSchema::table('customers', function (Blueprint $table) {\n  // Filter by email (login, search)\n  $table->unique('email');\n  \n  // Sorting\n  $table->index('created_at');\n});\n\nSchema::table('order_items', function (Blueprint $table) {\n  // Fast JOINs\n  $table->index('order_id');\n  $table->index('product_id');\n});\n```\n\n**EXPLAIN ANALYZE**\n\n```php\n// Before index: Full table scan (slow)\nDB::statement('EXPLAIN ANALYZE SELECT * FROM orders WHERE status = \"pending\" AND created_at > NOW() - INTERVAL 7 DAY');\n\n// After index: Index range scan (fast)\n```\n\n## Strategy 5: Efficient Serialization\n\n```php\n// ❌ WRONG - Loads everything\nRoute::get('/api/products', function () {\n  return Product::with('category', 'reviews', 'inventory')\n    ->get();\n  // Response: 2MB for 100 products\n});\n\n// ✅ CORRECT - Select only what's needed\nRoute::get('/api/products', function () {\n  return Product::select('id', 'name', 'price', 'category_id')\n    ->with([\n      'category:id,name',\n      'reviews:id,product_id,rating',\n    ])\n    ->get();\n  // Response: 150KB for 100 products\n});\n```\n\n## Performance Checklist\n\n**Immediate (before Monday):**\n• Remove N+1 queries with `with()`\n• Use `select()` to fetch only necessary columns\n• Add indexes on `where` and `orderBy` columns\n• Cache homepage with `Cache::remember()`\n\n**Short term (next week):**\n• Move synchronous jobs to queues\n• Implement read models for dashboards\n• Use Redis for session cache\n• Monitor with Blackfire/Debugbar\n\n**Medium term (next month):**\n• Database replication (read replicas)\n• CDN for assets\n• Elasticsearch for complex searches\n• Horizontal scaling with load balancer\n\n## When You Actually Need to Rewrite\n\n**Rewrite ONLY if:**\n• Laravel reaches 100K RPS and needs 200K\n• Sub-millisecond latency required (trading, gaming)\n• Real-time problem (24/7 websockets)\n• Team prefers other language (not a perf decision)\n\n**NOT a case for rewriting:**\n• App is slow but unmeasured\n• \"Go is faster than PHP\"\n• Reimplementing from scratch \"correctly\"\n• Because new tech is cool\n\n## Summary\n\n**90% of the time, scaling Laravel is:**\n1. Find bottleneck with profiler\n2. Cache in 3 layers (browser, app, db)\n3. Move sync work → async with queues\n4. Read models for aggregations\n5. Smart indexes in database\n\n**Time**: 1-2 sprints\n**Cost**: 0 (use existing infra)\n**Risk**: Low (incremental changes)\n\nRewriting takes 6 months, costs 3x more, and you discover Node is also slow without caching.",
      },
    },
    {
      slug: "portfolio-project-ideas",
      title: {
        pt: "Projetos para portfólio: ideias além da calculadora",
        en: "Portfolio projects: ideas beyond the calculator",
      },
      date: "2026-04-19",
      read: "14 min",
      kicker: { pt: "Carreira", en: "Career" },
      summary: {
        pt: "Seu portfólio não impressiona ninguém com CRUD de notas ou calculadora. Ideia: cria algo que resolve seu problema real. Web scraper, analisador de GitHub, simulador, exportador de dados. O projeto de verdade não é aquele que funciona — é aquele que é honesto sobre suas limitações e desafios arquiteturais.",
        en: "Your portfolio won't impress anyone with a notes CRUD or calculator. Idea: build something that solves your real problem. Web scraper, GitHub analyzer, simulator, data exporter. The real project isn't the one that works — it's the one that's honest about its architectural challenges.",
      },
      excerpt: {
        pt: "## O Problema: Portfólios Genéricos\n\nVocê vê portfolios cheios de:\n• Todo app\n• Calculadora\n• CRUD de produtos\n• Blog com Markdown\n• Chat em tempo real\n\n**Ninguém diferencia disso.** Todo dev fez. Recrutador vê 50 desses por semana.\n\n## O Que Impressiona (De Verdade)\n\n### 1. Soluciona Seu Problema Real\n\n**Exemplos que funcionam:**\n\n```\n• Scraper que monitora preços de imóveis no seu bairro\n  → Envia alerta no Telegram quando cai abaixo de X\n  → Redux na sua vida, que dirá no código\n\n• Analisador de GitHub que extrai padrões de bugs\n  → Lê issues + PRs do teu repo\n  → Agrupa por tipo (memory leak, race condition, off-by-one)\n  → Gera relatório: \"Você cai 3x em boundary conditions\"\n\n• Simulador de filas com múltiplas estratégias\n  → Compara SJF vs Priority vs Fair Queueing\n  → Visualiza em tempo real qual é mais eficiente pra seu caso\n  → Exporta gráficos pro relatório\n\n• Exportador de dados pessoais\n  → Puxa historico do seu email, Google Drive, Telegram\n  → Reorganiza em timeline\n  → Gera \"seu ano em números\": quantas msgs, fotos, contribuições\n  → Cria um PDF bonito\n\n• Monitorizador de CI/CD customizado\n  → Hookeia GitHub Actions\n  → Classifica falhas (test, lint, build, deploy)\n  → Sugere quem deve revisar baseado em expertise\n  → Notifica no Slack com contexto\n```\n\n### 2. Mostra Trade-offs Arquiteturais\n\nO que diferencia devs sênior:\n\n```\n❌ FRACO: \"Funciona, é rápido, código lindo\"\n\n✅ FORTE:\n• \"Função X é lenta em 2% dos casos porque [motivo]\"\n• \"Comecei com Redis, mas descobri que [problema]\"\n• \"Usando Postgres JSONB aqui porque queries aninhadas iam matar perf\"\n• \"Troquei event sourcing por CQRS light porque [trade-off]\"\n• \"Cacheei agressivamente porque latência sub-100ms é crítica\"\n```\n\n**Projeto com histórico de decisões > projeto perfeito nascido pronto**\n\n## Categorias de Projetos Que Funcionam\n\n### Categoria A: Data + Scraping\n\n**Ideia 1: Scraper com análise**\n\n```php\n// Monitora preços de produtos no Mercado Livre / OLX\n// Cada produto tem histórico de preço\n// Alertas por Telegram: \"Queda 20% nos últimos 7 dias\"\n\nclass ProductMonitor\n{\n  public function scrapeAndAnalyze(): void\n  {\n    $products = $this->scraper->fetch();\n    \n    foreach ($products as $product) {\n      $history = PriceHistory::where('product_id', $product->id)\n        ->orderByDesc('checked_at')\n        ->limit(30)\n        ->get();\n      \n      $percentChange = $this->calculateTrend($history);\n      \n      if ($percentChange < -20) {\n        $this->notifyTelegram(\"Preço caiu {$percentChange}%: {$product->name}\");\n      }\n      \n      PriceHistory::create([\n        'product_id' => $product->id,\n        'price' => $product->price,\n        'checked_at' => now(),\n      ]);\n    }\n  }\n}\n```\n\n**Ideia 2: Analisador de repositório GitHub**\n\n```php\n// Puxa issues, PRs, commits do teu repo\n// Identifica padrões de bugs\n\nclass GitHubAnalyzer\n{\n  public function analyzeErrorPatterns(): array\n  {\n    $issues = $this->github->fetchIssues('vitorgabrieldev/laravel-fintech');\n    \n    $patterns = [];\n    foreach ($issues as $issue) {\n      // Classifica por tipo\n      if (str_contains($issue->title, ['null', 'undefined', 'NaN'])) {\n        $patterns['null-safety'][]= $issue;\n      }\n      if (str_contains($issue->body, ['race', 'concurrent', 'transaction'])) {\n        $patterns['concurrency'][] = $issue;\n      }\n      if (str_contains($issue->body, ['loop', 'iteration', 'index'])) {\n        $patterns['off-by-one'][] = $issue;\n      }\n    }\n    \n    return [\n      'total_issues' => count($issues),\n      'patterns' => $patterns,\n      'top_issue' => $this->getMostCommonPattern($patterns),\n      'recommendation' => \"Seu maior problema é {$patterns.top_issue}, considere...\",\n    ];\n  }\n}\n```\n\n### Categoria B: Simuladores e Visualizadores\n\n**Ideia 3: Simulador de filas**\n\n```php\n// Compara diferentes estratégias de escalonamento\n\nclass QueueSimulator\n{\n  public function compareStrategies(array $jobs): array\n  {\n    return [\n      'fifo' => $this->simulateFIFO($jobs),\n      'sjf' => $this->simulateSJF($jobs),\n      'priority' => $this->simulatePriority($jobs),\n      'fair_queueing' => $this->simulateFairQueueing($jobs),\n    ];\n  }\n  \n  private function simulateSJF(array $jobs): array\n  {\n    $sorted = collect($jobs)->sortBy('duration');\n    $totalWaitTime = 0;\n    $timeline = [];\n    \n    foreach ($sorted as $job) {\n      $totalWaitTime += $job->duration;\n      $timeline[] = [\n        'job' => $job->name,\n        'started_at' => $totalWaitTime - $job->duration,\n        'finished_at' => $totalWaitTime,\n      ];\n    }\n    \n    return [\n      'strategy' => 'Shortest Job First',\n      'avg_wait_time' => $totalWaitTime / count($jobs),\n      'timeline' => $timeline,\n    ];\n  }\n}\n```\n\n**Ideia 4: Visualizador de transações bancárias**\n\n```\n• Importa CSV do banco\n• Categoriza automaticamente (Alimentação, Transporte, etc)\n• Mostra gráficos: gastos por mês, categoria, dia da semana\n• Identifica anomalias: \"Você gastou 3x em Restaurant em Março\"\n• Prediz: \"Seu padrão de gastos = R$2.500/mês\"\n• Exporta insights em PDF\n```\n\n### Categoria C: Ferramentas Pessoais\n\n**Ideia 5: Exportador de dados pessoais**\n\n```php\n// Cria arquivo único com seu histórico de vida digital\n\nclass PersonalDataExporter\n{\n  public function export(): void\n  {\n    $timeline = [];\n    \n    // Gmail\n    $emails = $this->gmail->fetch();\n    foreach ($emails as $email) {\n      $timeline[] = [\n        'date' => $email->date,\n        'type' => 'email',\n        'data' => $email->subject,\n      ];\n    }\n    \n    // GitHub contributions\n    $commits = $this->github->fetchCommits();\n    foreach ($commits as $commit) {\n      $timeline[] = [\n        'date' => $commit->created_at,\n        'type' => 'contribution',\n        'data' => $commit->message,\n      ];\n    }\n    \n    // Telegram messages\n    $messages = $this->telegram->fetchMessages();\n    foreach ($messages as $msg) {\n      $timeline[] = [\n        'date' => $msg->date,\n        'type' => 'message',\n        'data' => $msg->text,\n      ];\n    }\n    \n    $sorted = collect($timeline)->sortBy('date');\n    \n    // Gera stats\n    $stats = [\n      'total_emails' => count($emails),\n      'total_commits' => count($commits),\n      'total_messages' => count($messages),\n      'date_range' => $sorted->first()['date'] . ' to ' . $sorted->last()['date'],\n      'busiest_month' => $this->getBusiestMonth($sorted),\n      'commits_per_day' => count($commits) / $sorted->count(),\n    ];\n    \n    $this->pdf->generate('annual-report.pdf', $stats, $sorted);\n  }\n}\n```\n\n### Categoria D: Análise e Monitoring\n\n**Ideia 6: Monitor de CI/CD inteligente**\n\n```php\n// Hookeia GitHub Actions, classifica falhas\n\nclass CIPipeline\n{\n  public function classifyFailure(array $log): array\n  {\n    $content = implode('\\n', $log);\n    \n    return match(true) {\n      str_contains($content, ['FAILED', 'AssertionError']) => [\n        'type' => 'test',\n        'severity' => 'critical',\n        'action' => 'Seu teste falhou',\n      ],\n      str_contains($content, ['syntax error', 'parse error']) => [\n        'type' => 'lint',\n        'severity' => 'blocker',\n        'action' => 'Erro de sintaxe, não vai passar',\n      ],\n      str_contains($content, ['Connection refused', 'timeout']) => [\n        'type' => 'infra',\n        'severity' => 'warning',\n        'action' => 'Problema de conexão (ephemeral?)',\n      ],\n      str_contains($content, ['FATAL', 'OOM', 'panic']) => [\n        'type' => 'runtime',\n        'severity' => 'critical',\n        'action' => 'App crashou',\n      ],\n      default => [\n        'type' => 'unknown',\n        'severity' => 'info',\n        'action' => 'Falha desconhecida, inspeciona logs',\n      ]\n    };\n  }\n}\n```\n\n## Checklist: Seu Projeto Impressiona?\n\n**Responde SIM a:**\n\n• Resolveu um problema real seu (ou de alguém)\n• Tem um histórico de decisões documentadas\n• Mostra trade-offs: \"Usei Redis aqui porque X, mas Y é problema\"\n• Tem dados de verdade (não fake data)\n• Documenta limitações: \"Scaler pra 100K items é problema\"\n• Mostra iteração: \"Comecei com Y, mudei pra Z porque\"\n\n**Evita:**\n\n• \"Built with React, Firebase, Tailwind\" (genérico)\n• Código perfeito mas sem história\n• Feature creep (1.000 features, nenhuma bem feita)\n• Sem documentação de desafios\n• Tutorial-driven (copiou do YouTube)\n\n## Exemplo: Risklog (Meu)\n\nPor que impressiona:\n\n```\n✅ Problema real: Deploy silencioso é risco\n✅ Trade-off: Analisar diff é pesado, mas cacheamos resultados\n✅ Limitação honesta: Não detecta race conditions em runtime\n✅ Métrica: Reduziu post-deploy scrambles 62%\n✅ Arquitetura: Event stream de diffs → classificação → score\n✅ Decisão documentada: Elegemos Postgres JSONB porque queries aninhadas\n```\n\n## Próximos Passos\n\n**Semana 1:** Escolhe um problema seu\n**Semana 2-4:** Implementa solução básica (funciona, não perfeito)\n**Semana 5:** Documenta desafios: \"Por quê fiz assim?\"\n**Semana 6:** Mostra no README:\n• O problema\n• Sua solução\n• Trade-offs\n• Limitações\n• Como você iteraria\n\n**Resultado:** Portfólio que diz \"eu penso sobre arquitetura\", não \"eu sigo tutoriais\".",
        en: "## The Problem: Generic Portfolios\n\nYou see portfolios full of:\n• Todo app\n• Calculator\n• Product CRUD\n• Blog with Markdown\n• Real-time chat\n\n**No one differentiates from this.** Every dev did it. Recruiter sees 50 of these per week.\n\n## What Actually Impresses\n\n### 1. Solves Your Real Problem\n\n**Examples that work:**\n\n```\n• Scraper that monitors real estate prices in your neighborhood\n  → Sends Telegram alert when below X\n  → Solves your actual life problem, not a tutorial\n\n• GitHub analyzer that extracts bug patterns\n  → Reads issues + PRs from your repo\n  → Groups by type (memory leak, race condition, off-by-one)\n  → Report: \"You fall 3x into boundary conditions\"\n\n• Queue simulator with multiple strategies\n  → Compares SJF vs Priority vs Fair Queueing\n  → Visualizes real-time which is most efficient for your case\n  → Exports charts for report\n\n• Personal data exporter\n  → Pulls history from email, Google Drive, Telegram\n  → Reorganizes into timeline\n  → Generates \"your year in numbers\": messages, photos, contributions\n  → Creates beautiful PDF\n\n• Custom CI/CD monitor\n  → Hooks GitHub Actions\n  → Classifies failures (test, lint, build, deploy)\n  → Suggests who should review based on expertise\n  → Notifies Slack with context\n```\n\n### 2. Shows Architectural Trade-offs\n\nWhat differentiates senior devs:\n\n```\n❌ WEAK: \"Works, it's fast, code is clean\"\n\n✅ STRONG:\n• \"Function X is slow in 2% of cases because [reason]\"\n• \"Started with Redis, discovered [problem]\"\n• \"Using Postgres JSONB because nested queries would kill perf\"\n• \"Switched from event sourcing to CQRS light because [trade-off]\"\n• \"Aggressively cached because sub-100ms latency is critical\"\n```\n\n**Project with decision history > perfect project born perfect**\n\n## Categories of Working Projects\n\n### Category A: Data + Scraping\n\n**Idea 1: Scraper with analysis**\n\n```php\n// Monitors product prices on Mercado Livre / OLX\n// Each product has price history\n// Alerts via Telegram: \"20% drop in last 7 days\"\n\nclass ProductMonitor\n{\n  public function scrapeAndAnalyze(): void\n  {\n    $products = $this->scraper->fetch();\n    \n    foreach ($products as $product) {\n      $history = PriceHistory::where('product_id', $product->id)\n        ->orderByDesc('checked_at')\n        ->limit(30)\n        ->get();\n      \n      $percentChange = $this->calculateTrend($history);\n      \n      if ($percentChange < -20) {\n        $this->notifyTelegram(\"Price dropped {$percentChange}%: {$product->name}\");\n      }\n      \n      PriceHistory::create([\n        'product_id' => $product->id,\n        'price' => $product->price,\n        'checked_at' => now(),\n      ]);\n    }\n  }\n}\n```\n\n**Idea 2: GitHub repository analyzer**\n\n```php\n// Pulls issues, PRs, commits from your repo\n// Identifies bug patterns\n\nclass GitHubAnalyzer\n{\n  public function analyzeErrorPatterns(): array\n  {\n    $issues = $this->github->fetchIssues('vitorgabrieldev/laravel-fintech');\n    \n    $patterns = [];\n    foreach ($issues as $issue) {\n      // Classifies by type\n      if (str_contains($issue->title, ['null', 'undefined', 'NaN'])) {\n        $patterns['null-safety'][] = $issue;\n      }\n      if (str_contains($issue->body, ['race', 'concurrent', 'transaction'])) {\n        $patterns['concurrency'][] = $issue;\n      }\n      if (str_contains($issue->body, ['loop', 'iteration', 'index'])) {\n        $patterns['off-by-one'][] = $issue;\n      }\n    }\n    \n    return [\n      'total_issues' => count($issues),\n      'patterns' => $patterns,\n      'top_issue' => $this->getMostCommonPattern($patterns),\n      'recommendation' => \"Your biggest problem is {$patterns.top_issue}, consider...\",\n    ];\n  }\n}\n```\n\n### Category B: Simulators and Visualizers\n\n**Idea 3: Queue simulator**\n\n```php\n// Compares different scheduling strategies\n\nclass QueueSimulator\n{\n  public function compareStrategies(array $jobs): array\n  {\n    return [\n      'fifo' => $this->simulateFIFO($jobs),\n      'sjf' => $this->simulateSJF($jobs),\n      'priority' => $this->simulatePriority($jobs),\n      'fair_queueing' => $this->simulateFairQueueing($jobs),\n    ];\n  }\n  \n  private function simulateSJF(array $jobs): array\n  {\n    $sorted = collect($jobs)->sortBy('duration');\n    $totalWaitTime = 0;\n    $timeline = [];\n    \n    foreach ($sorted as $job) {\n      $totalWaitTime += $job->duration;\n      $timeline[] = [\n        'job' => $job->name,\n        'started_at' => $totalWaitTime - $job->duration,\n        'finished_at' => $totalWaitTime,\n      ];\n    }\n    \n    return [\n      'strategy' => 'Shortest Job First',\n      'avg_wait_time' => $totalWaitTime / count($jobs),\n      'timeline' => $timeline,\n    ];\n  }\n}\n```\n\n**Idea 4: Bank transaction visualizer**\n\n```\n• Imports bank CSV\n• Auto-categorizes (Food, Transport, etc)\n• Shows charts: spending by month, category, day of week\n• Identifies anomalies: \"You spent 3x on restaurants in March\"\n• Predicts: \"Your pattern = R$2,500/month\"\n• Exports insights to PDF\n```\n\n### Category C: Personal Tools\n\n**Idea 5: Personal data exporter**\n\n```php\n// Creates single file with your digital life history\n\nclass PersonalDataExporter\n{\n  public function export(): void\n  {\n    $timeline = [];\n    \n    // Gmail\n    $emails = $this->gmail->fetch();\n    foreach ($emails as $email) {\n      $timeline[] = [\n        'date' => $email->date,\n        'type' => 'email',\n        'data' => $email->subject,\n      ];\n    }\n    \n    // GitHub contributions\n    $commits = $this->github->fetchCommits();\n    foreach ($commits as $commit) {\n      $timeline[] = [\n        'date' => $commit->created_at,\n        'type' => 'contribution',\n        'data' => $commit->message,\n      ];\n    }\n    \n    // Telegram messages\n    $messages = $this->telegram->fetchMessages();\n    foreach ($messages as $msg) {\n      $timeline[] = [\n        'date' => $msg->date,\n        'type' => 'message',\n        'data' => $msg->text,\n      ];\n    }\n    \n    $sorted = collect($timeline)->sortBy('date');\n    \n    // Generate stats\n    $stats = [\n      'total_emails' => count($emails),\n      'total_commits' => count($commits),\n      'total_messages' => count($messages),\n      'date_range' => $sorted->first()['date'] . ' to ' . $sorted->last()['date'],\n      'busiest_month' => $this->getBusiestMonth($sorted),\n      'commits_per_day' => count($commits) / $sorted->count(),\n    ];\n    \n    $this->pdf->generate('annual-report.pdf', $stats, $sorted);\n  }\n}\n```\n\n### Category D: Analysis and Monitoring\n\n**Idea 6: Smart CI/CD monitor**\n\n```php\n// Hooks GitHub Actions, classifies failures\n\nclass CIPipeline\n{\n  public function classifyFailure(array $log): array\n  {\n    $content = implode('\\n', $log);\n    \n    return match(true) {\n      str_contains($content, ['FAILED', 'AssertionError']) => [\n        'type' => 'test',\n        'severity' => 'critical',\n        'action' => 'Your test failed',\n      ],\n      str_contains($content, ['syntax error', 'parse error']) => [\n        'type' => 'lint',\n        'severity' => 'blocker',\n        'action' => 'Syntax error, won\\'t pass',\n      ],\n      str_contains($content, ['Connection refused', 'timeout']) => [\n        'type' => 'infra',\n        'severity' => 'warning',\n        'action' => 'Connection problem (ephemeral?)',\n      ],\n      str_contains($content, ['FATAL', 'OOM', 'panic']) => [\n        'type' => 'runtime',\n        'severity' => 'critical',\n        'action' => 'App crashed',\n      ],\n      default => [\n        'type' => 'unknown',\n        'severity' => 'info',\n        'action' => 'Unknown failure, inspect logs',\n      ]\n    };\n  }\n}\n```\n\n## Checklist: Does Your Project Impress?\n\n**Answer YES to:**\n\n• Solved a real problem (yours or someone\\'s)\n• Has documented decision history\n• Shows trade-offs: \"Used Redis here because X, but Y is a problem\"\n• Has real data (not fake data)\n• Documents limitations: \"Scaling to 100K items is a problem\"\n• Shows iteration: \"Started with Y, changed to Z because\"\n\n**Avoid:**\n\n• \"Built with React, Firebase, Tailwind\" (generic)\n• Perfect code but no story\n• Feature creep (1,000 features, none well done)\n• No documentation of challenges\n• Tutorial-driven (copied from YouTube)\n\n## Example: Risklog (Mine)\n\nWhy it impresses:\n\n```\n✅ Real problem: Silent deploy is risky\n✅ Trade-off: Analyzing diff is heavy, but we cache results\n✅ Honest limitation: Doesn\\'t detect runtime race conditions\n✅ Metric: Reduced post-deploy scrambles 62%\n✅ Architecture: Event stream of diffs → classification → score\n✅ Documented decision: Chose Postgres JSONB because nested queries would kill perf\n```\n\n## Next Steps\n\n**Week 1:** Pick a problem you have\n**Week 2-4:** Build basic solution (works, not perfect)\n**Week 5:** Document challenges: \"Why did I do this?\"\n**Week 6:** Show in README:\n• The problem\n• Your solution\n• Trade-offs\n• Limitations\n• How you\\'d iterate\n\n**Result:** Portfolio that says \"I think about architecture\", not \"I follow tutorials\".",
      },
    },
  ],

  playground: [
    {
      title: "Bezier playground",
      kind: "canvas",
      note: "Drag handles, read the easing curve.",
    },
    {
      title: "Tokenizer viewer",
      kind: "widget",
      note: "Paste text, inspect BPE tokens live.",
    },
    {
      title: "CI/CD graph",
      kind: "graph",
      note: "Animated pipeline runs with risk tags.",
    },
  ],

  oss: [
    {
      repo: "laravel-fintech-core",
      owner: "vitorgabrieldev",
      desc: {
        pt: "Core bancário com ledger de partidas dobradas, contas multi-tenant e transações auditáveis.",
        en: "Banking core with double-entry ledger, multi-tenant accounts and auditable transactions.",
      },
      stars: 42,
      lang: "PHP",
      kind: { pt: "Autor", en: "Author" },
    },
    {
      repo: "risklog",
      owner: "vitorgabrieldev",
      desc: {
        pt: "Camada de CI/CD que resume deploys em linguagem natural — o que quebrou, por quê, quem deve olhar.",
        en: "CI/CD layer that summarises deploys in natural language — what broke, why, who should look.",
      },
      stars: 18,
      lang: "TypeScript",
      kind: { pt: "Autor", en: "Author" },
    },
    {
      repo: "matetatica-web",
      owner: "vitorgabrieldev",
      desc: {
        pt: "Plataforma de ensino para matemática adaptativa — motor de exercícios + repetição espaçada.",
        en: "Teaching platform for adaptive math — exercise engine + spaced repetition.",
      },
      stars: 11,
      lang: "Laravel",
      kind: { pt: "Autor", en: "Author" },
    },
    {
      repo: "laravel/framework",
      owner: "laravel",
      desc: {
        pt: "Pequenos fixes e PRs de docs upstream — type hints, casos extremos do query builder.",
        en: "Small fixes and docs PRs upstream — type hints, query builder edge cases.",
      },
      stars: null,
      lang: "PHP",
      kind: { pt: "Contribuidor", en: "Contributor" },
    },
    {
      repo: "spatie/laravel-permission",
      owner: "spatie",
      desc: {
        pt: "Bugfix para invalidação de cache de permissão com escopo de time em setups multi-guard.",
        en: "Bugfix for team-scoped permission cache invalidation in multi-guard setups.",
      },
      stars: null,
      lang: "PHP",
      kind: { pt: "Contribuidor", en: "Contributor" },
    },
    {
      repo: "dotfiles",
      owner: "vitorgabrieldev",
      desc: {
        pt: "Meu config de neovim + tmux + zsh. Nada revolucionário, só honesto.",
        en: "My neovim + tmux + zsh config. Nothing revolutionary, just honest.",
      },
      stars: 7,
      lang: "Lua",
      kind: { pt: "Autor", en: "Author" },
    },
  ],
};
